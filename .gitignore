node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

test-results
playwright-report
source
_screenshots

# V8 heap snapshots
*.heapsnapshot

# Certificates and private keys (Let's Encrypt artifacts, self-signed dev certs,
# Windows certificate bundles). Never commit these - even dev-only material can
# tip an attacker about deployment shape and leak ACL-relevant identifiers.
*.pem
*.key
*.crt
*.pfx
*.p12