# Changelog

All notable changes to `svelte-realtime-demo` will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

---

## [Unreleased]

### Added

- **Official ecosystem links at the demo front door.** The README now identifies
  the GitHub owner, canonical documentation, hosted demo, and the ecosystem-owned
  `svti.me` runtime-help redirect domain.

- **Connection diagnostics on the e2e connection wait, plus a sweep probe.** `waitForWS` installs a probe for the duration of the wait that records the connection-status timeline and every WebSocket the page opens, including each socket's URL and how it ended; a timeout now reports that timeline instead of only naming the selector it gave up on. It also logs a `[ws-slow]` line for any connect that passes but exceeds 2000ms, so a run yields evidence about its slow tail rather than only about outright failures. The status timeline is sampled rather than observed because the navbar swaps the whole icon element between `Wifi` and `WifiOff` across a status change, and a `MutationObserver` bound to the old node would go deaf at exactly the transition worth seeing. The socket URL is what separates "the app never connected" from "the app's connection was ignored" - in dev the page also holds Vite's HMR socket, which opens immediately and never closes, and without the URL the two are indistinguishable. New `test/e2e/_ws-connect-probe.spec.js` (diagnostics tier) cycles the demo routes and reports the time-to-connected distribution, lost attempts, and per-route worst cases.

- **The connection wait reports a page that never booted, instead of the socket it never opened.** A wait now also watches, from Playwright's side, the requests the page fails to load and the errors it raises, and names the fault outright: an `/_app/immutable/` asset that fails (`ERR_NO_BUFFER_SPACE` and friends) or a `Failed to fetch dynamically imported module` error is reported as `PAGE NEVER HYDRATED`, with the failing URL. The in-page probe cannot see any of this, because a page whose client bundle never loaded has nothing left to ask; the evidence exists only outside it. Where there is no direct evidence the report falls back to the shape a dead bundle leaves behind - no app socket constructed and a status that never left its server-rendered value - and hedges that reading as inferred, since "never booted" and "booted but could not connect" have opposite fixes. A wait that passes while a request failed underneath it now logs `[ws-fault]`: the browser retried an asset and won, which is the same exhaustion that loses the race when it does not, and a pass/fail gate discards exactly that near miss.

- **A page that provably never loaded its client bundle is reloaded once, instead of failing the run.** Two things keep this a recovery rather than a way of making a real failure quiet, which is the same objection that got a timeout increase rejected on this problem earlier. It fires only on a *fact* - a failed `/_app/` script request or a dynamic-import error - never on a bare timeout, so a genuine 15-second connect stall still fails exactly as loudly as before; and a page whose bundle never ran has no client-side state to lose, so the reload is safe even mid-test, since anything the spec established is either server-side and survives or was never created. Every reload logs `[ws-rehydrate]`, on both the recovery and the give-up path, so a rising rate stays visible rather than becoming the new invisible flake. The budget is one reload (`WS_HYDRATE_RELOADS`): a transient clears on the first, and a second would only be papering over something persistent, which is a finding. Covered in both directions - a one-off dead bundle recovers and the run continues; a persistent one spends the budget and still fails with the asset named.

- **Every positive connection wait routes through `waitForWS`.** Nine `.text-success` waits across `performance.spec.js`, `websocket.spec.js`, `demos-forget.spec.js` and `demos-forget.cluster.spec.js` had been hand-rolled, so they carried none of the diagnostics above - which is why the one connection failure of the last validation run produced no probe report at all. The negative waits (`toHaveCount(0)`, asserting a page is disconnected) are deliberately left alone. `waitForWS` also returns its probe snapshot now, which is what lets a test assert against it.

- **A multiplayer presence probe that measures convergence instead of asserting it once.** New `test/e2e/_mp-presence-probe.spec.js` (diagnostics tier) pairs two fresh browser contexts in the `live.multiplayer` room and waits for each to see the other, in a loop, reporting the failure rate with a Wilson interval and the convergence distribution among successes. On a failure it dumps both sides' rosters, because an empty roster cannot by itself distinguish a join that was never published from one delivered in only one direction, and those have different fixes. That capture is what identified the fault behind the intermittent gate failures: every failure is asymmetric, so the blind side's own join reached its peer while the peer's presence never arrived at all.

- **The control-colour vocabulary is written down where colours get chosen.** `btn-warning` is the dress for a control that induces chaos or failure on purpose - a retry storm, a shed simulation, a deliberately failing order - and nine of the eleven that wear it are exactly that, so the yellow reads as a promise that pressing this makes something go wrong deliberately. It is not the dress for a destructive action; those wear `btn-outline btn-error` and go through the confirm step. Two controls used it for something else and have been brought into line, because a vocabulary that holds except twice predicts nothing: the board's "Shuffle notes" FAB is now neutral, which still tells it apart from its siblings without borrowing the chaos reading, and the notification inbox's "Dismiss" is now ghost so the success-dressed "Got it" carries the pair on its own. Neither induces failure. The FAB menu test that located Shuffle by `.btn-warning` now locates every action by the label a visitor reads, so it pins the menu's contents instead of its dress - asserting the colour meant a renamed action passed while a re-dressed one failed.

- **A bare checkbox had no target but itself, and the floor never reached it.** The coarse-pointer policy puts the 44px floor on the label that wraps a checkbox or toggle, which leaves the drawn control exactly as designed - but a control with no label wrapping it has nothing else to hit, so the todo list's per-row checkbox stayed 24px on a phone while the policy claimed otherwise. It now has a label of its own, wrapping only the control, so the floor lands on a real target and the drawn box keeps its designed size; the label rule gained a width floor too, since a label wrapping nothing but a control has no text to make it wide, and a 44px-tall target 24px wide is not a 44px target. Extending the hit area with a pseudo-element was tried first and is inert - these inputs do not hit-test an `::after`, so the overlay is invisible to a finger as well as to the eye, and a label is the mechanism the platform actually provides. Its own touch test had blessed 24x24 as "the WCAG AA minimum, not the 44px button floor" - that is the AA floor standing in for the policy this app actually states - and now pins both halves: the drawn control stays under 44px, and a point 20px from its centre still belongs to the control. Either assertion alone passes for the wrong reason. A stale comment claiming these controls were deliberately excluded, on a rendering mechanism that turned out not to exist, is corrected rather than left contradicting the rules beneath it.

- **The name sweep could not reach two of the five controls it claimed to enforce.** A directory scan finds `/demos/chat`, never `/demos/chat/<roomId>`, so the chat composer sat outside the sweep while the sweep looked complete; deep paths reachable by URL are now listed explicitly and the sweep asserts they actually contributed controls, so a route that silently stopped rendering cannot pass as covered. The lobbies composer exists only after a lobby is joined and no URL reaches it, so the policy is enforced in the spec that performs the join.

- **The two re-dressed controls now have oracles, and the FAB tests stopped naming colours.** Changing Shuffle to neutral and Dismiss to ghost made the warning vocabulary coherent with nothing committed to keep it that way; both are now pinned as the *absence* of `btn-warning`, so the rule holds while the replacement hue stays a design choice. Three further FAB tests located their actions by `.btn-secondary`, `.btn-accent` and `.btn-info` - a renamed action passed as long as something kept the class - and now click by the label a visitor reads.

- **Undo's coverage of the board's layout actions is pinned as what it is.** The four FAB actions rewrite every note's position for every visitor with no confirm step, so their safety rests entirely on undo covering them - and it does not. A test now shuffles, presses Ctrl+Z, reloads, and asserts the shuffle survived and the previous arrangement did not come back.

- **The schema-evolution error state now has a test that can actually reach it.** The page gained an honest message when the state RPC fails - the registration snippet and the server-version chip come from that one call, and both are withheld rather than guessed at - but nothing exercised it, so the markup existed and no evidence said it ever rendered. The regression now closes the socket, which is what makes the RPC genuinely reject, and asserts the explanation appears, names its cause, and that *both* withheld things stay absent: the empty `<pre>` that was the original complaint, and the subtler one, a version chip reading "server version: 2" from the client default as though the server had confirmed it. Swallowing the error again fails the test with the element not found.

- **The rollback demo's mechanism note said the opposite of what the server does.** The note told visitors the handler throws `LiveError('FORCED', ...)` *immediately*, which stopped being true when a deliberate 400ms wait was added so the optimistic row is on screen long enough to see roll back. The page was teaching one thing while doing another, and the pause read as latency rather than a choice. It now states the wait, and why it exists: the failure is already artificial, so delaying it costs nothing real and is the only thing that makes the arc observable. A test pins the note against the behaviour - it must name the delay and must not claim an immediate throw - so the copy cannot drift back to contradicting the code. The number is now stated once: the handler, the note a visitor reads, and the regression all import `FORCED_FAIL_DELAY_MS` from one shared module. Three independent literals agreed until one of them moved, and nothing failed when they stopped agreeing - which is the same defect the note was written to prevent, sitting in the test that was supposed to catch it. Drift now fails; changing the constant moves all three together and passes, which is the point.

- **Two visitors on /demos/schema-evolution no longer settle on different numbers.** The counter publishes an absolute value from whichever replica served the click, and `merge: 'crud'` replaces by key as frames land - so two clicks on different replicas reached two clients in opposite orders, and the client that received the higher value first displayed the lower one *permanently*, because those two publishes were all there would ever be. The divergence was terminal, not a transient reorder. Both panels now display the newest observation of each counter instead of the last frame to arrive, ordered by `modifiedAt`, which is stamped inside the Redis script that performs the write - so every replica reads one clock regardless of which instance served the click. The envelope's `seq` cannot do this: it numbers per-subscriber delivery, so the same logical event carries a different number on each client. A same-millisecond tie is broken by the higher count, since a counter that only rises makes the larger value the later one, and a reset is ordered by the same rule rather than special-cased. The rule lives in `$lib/newest-wins` with unit coverage that replays the captured wire order on two independent clients and asserts they converge; removing the ordering comparison fails four of its five tests.

- **The narrow switcher's fade is now observed, not just its state machine.** The regression asserted the `data-overflow` attribute through all four transitions, which is the state that DRIVES the affordance rather than the affordance itself - every mask rule could be deleted and the test stayed green, leaving the finding unfixed and the pin passing. Each state now resolves the computed mask to the EDGES it fades and asserts the exact set: the right edge alone at the start, both in the middle, the left alone at the end, and none at all at desktop width where the strip does not scroll. Asserting only that a gradient exists, or only that the states differ from one another, is satisfied by a fade painted on the wrong edge - which tells a phone visitor the opposite of the truth about where the catalog continues. Disabling the mask rules fails it, and so does swapping the two of them.

- **A single error toast on /demos/todos-rollback never expired.** The removal timer compared object identity against the entry it had closed over, but `toasts` is `$state`, so the array holds a *proxy* of that object - the comparison matched nothing, the filter removed nothing, and the alert sat over the list it was reporting on until a reload. It hid because the coalescing path removes correctly by accident: its entry comes out of `toasts.find(...)` and is already the proxy, so only a lone error - one failure, never repeated - was stranded, and the burst case that the suite exercised always cleaned up. Removal now keys on the entry id. The spec asserted the toast appears and never that it leaves, which is why an indefinitely mounted alert passed; it now asserts both, and restoring the identity comparison fails it. It also holds the toast to its DECLARED lifetime rather than to a round number: ten seconds against a declared 3.5 accepts an alert outliving its contract threefold, and since the toast sits over the list it is reporting on, that lifetime is how long the demo obscures its own subject. The number is now one exported constant the component and the regression both read.

- **A top-K bar assertion sat exactly on its own rounding boundary.** `progress` is parsed out of `style.width`, which the browser serialises rounded to four decimals, and the assertion allowed a tolerance of 0.00005 - precisely the largest error that rounding can produce. Any share landing on the half-ulp failed: a tier lost this test to 19.53125 arriving as 19.5312. It now allows three decimals, which clears the representation by an order of magnitude and stays far tighter than any real mismatch, since computing the bar against the leader rather than the total moves it by whole percent.

- **The undo suite now says what it actually measures.** Every existing undo test reads the note count in the same tab that pressed the key, which is the one place a local snapshot restore is visible - so all of them pass unchanged whether or not undo reaches the server, and it does not. `notesStore.undo()` restores the client's own snapshot of the stream: it issues no call, publishes nothing, and a reload shows the note still deleted. A new test pins that, asserting both halves - the local view restores, and the deletion survives a reload - so the suite records the behaviour that is true rather than the one a shared board implies. It uses two notes deliberately: with one, the post-reload assertion would expect zero, which a page that has not finished loading satisfies just as well as a genuinely deleted note, and the assertion would pass without the server being consulted. When undo is made real or withdrawn, this test fails and has to be rewritten, which is the point of pinning it.

- **No control uses its placeholder as its only name.** Five controls had no accessible name at all - the demo filter on the landing page, the chat and lobby composers, the kanban add-card field, and the collab-editor textarea - leaving a placeholder as the only description of what they were for. That is not a stylistic point: a placeholder vanishes the moment the field has content, so the description disappears exactly when a visitor reviews what they typed; it is drawn in a muted colour that fails contrast by design; and it is not a dependable accessible name. Each now carries one, scoped where a page has several of the same control (`Add a card to {col.label}`, `Shared document, {mode} selections`). They are `aria-label`s rather than visible labels on purpose: these are compose fields sitting beside their own send button and under their own heading, where a visible label duplicates what is already on screen - the defect was the missing *name*, not missing visible text. New `test/e2e/accessible-names.spec.js` sweeps every route and fails naming any control whose only description is its placeholder, using the DOM's own `labels` association so a `for=` reference and a wrapping label are both honoured without re-implementing either rule. The other eleven placeholder-bearing controls already had names and were left alone.

- **A coarse-pointer control floor, stated once instead of per control.** Over a hundred controls sat below the 44px touch floor - 82 `btn-sm` and 21 `btn-xs` in source, and the sweep found daisyUI's *default* `.btn` and `.input` land on 40px too, so the shortfall was wider than the compact variants. The alternative was a `pointer-coarse:min-h-11` utility on each one, which costs a class on every control on every page, is invisible when it is missing, and silently fails to cover the next control anyone adds. One `@media (pointer: coarse)` block in `src/app.css` now sets the floor for `.btn`, `.input`, `.select`, `.file-input`, `.textarea` and `.tab`, plus a width floor for square icon buttons. It is unlayered on purpose: daisyUI's component rules sit in a cascade layer and unlayered styles beat layered ones whatever their specificity, so it needs no `!important` and no selector escalation. Nothing is added to the markup and no JavaScript runs. The selector controls are covered too, by the target a finger actually has to hit rather than by the drawn box: a `.range` is operated by dragging its own track, so nothing around it can stand in for it and the control carries the floor itself, while a `.checkbox` or `.toggle` is also operated by its label, so the floor goes on the label and the drawn control is left exactly as designed. Only a direct child counts, since a label wrapping a row of controls is not the target for any one of them. New `test/e2e/touch-targets.spec.js` sweeps every route for controls under the floor and names each one with its page, size and classes; it discovers routes from the filesystem so a new demo is covered the moment it exists, and it asserts it actually measured controls, because a sweep that finds nothing would otherwise report success.

- **The checkout retry lockout is latched, not sampled.** The assertion that `Place Order` disables during a retry storm was racy by construction rather than by bad luck: an in-page `MutationObserver` timed that window at 7-10ms (p50 9ms) across ten runs, and `toBeDisabled()` cannot evaluate until the click has round-tripped out of the browser - so under full-tier load the window closes before the first poll and every later one sees the settled state, which is a test that can only fail. An observer installed before the click now latches the transition, which cannot miss it however short it is, and the wait settles on the storm finishing so the latch is read after the window rather than before it opened. It still fails when the lockout is absent: removing `disabled={busy}` from the button makes it fail on that assertion with that message.

- **The rehydrate reload is gated on the entry bundle, not on any failed script.** The predicate that authorises a mid-test reload accepted every non-aborted request whose resource type was `script`, plus anything matching `/_app/**.js`. That is not proof the client never booted: a page whose JavaScript is already running can lose an unrelated or lazily-injected script, and on such a page the reload destroys whatever state the spec had established - so a genuine connection failure was being relabelled `PAGE NEVER HYDRATED` and its retry budget burned, which is the timeout bump wearing a disguise. It now matches only `/_app/immutable/entry/<name>.js`, the one request whose failure cannot mean anything else. Route chunks are not a gap: when one fails the browser raises "failed to fetch dynamically imported module", and the existing error branch already catches that with proof of its own. Covered by a test in the direction the previous two could not see - both of those kill the entry bundle, so both describe pages that genuinely never booted, and neither can observe a false positive. The new one boots the page, breaks its socket, fails an unrelated script beside it, and asserts the page is not reloaded and not misreported; it fails against the old predicate, which reloads it.

- **A failed peer wait now names which side lost the join.** `waitForPeers` reported only `Expected 1, Received 0`, which is equally true of five states that have different causes and different owners, so every occurrence cost a manual re-derivation from screenshots. It now dumps both rosters and names the shape: one side blind (the join did reach the room, since the other side holds it, so one direction of the fan-out was lost), both sides blind, a side not rendering its own entry (identity or hydration, not delivery at all), entries present but carrying a name that does not match, or a roster that converged after the budget expired and is therefore latency. The roster reader is shared with the presence probe instead of duplicated. A regression test forces the failure and pins *which* side the verdict blames: a description that is only ever read when the suite is already red gets believed, and asserting the word "asymmetric" alone passes happily with the two sides swapped.

- **15 new demo pages - the gallery grows from 19 to 34, covering the full 0.6 primitive surface.** Each demo is one module in `src/live/demos/`, one page in `src/routes/demos/`, and one Playwright spec in `test/e2e/`: `collab-editor` (CRDT-anchored selections vs raw offsets on one shared `live.doc`), `multiplayer` (full-surface room: cursors, advisory locks, typing, reactions), `kanban` (shared CRDT board, zero RPC handlers), `offline` (IndexedDB-persisted mutation queue + idempotent replay), `arena` (`live.smooth({ interest })` area-of-interest culling with LOD bands), `shooter` (`live.smooth({ hitTest })` lag-compensated hits with a latency slider), `lobbies` (`live.room` browser with `meta`, ownership succession, and `shortCodes()`), `tenants` (`realtime({ tenant })` strict per-connection isolation), `flags` (`live.flag` cluster-replayed feature flags), `alarms` (Redis-durable `ctx.setAlarm` one-shots that survive restarts), `forget` (`live.forget` erasure with a per-surface audit), `privacy` (`live.aggregate` k-anonymity + differential-privacy hybrid), `ops` (`introspect()` dashboard), `outbound-webhooks` (`live.webhooks.outbound` sign / retry / dead-letter / replay), and `phases` (attach lifecycle + `ctx.batch` atomicity across an await boundary). The game demos keep their simulation in pure `.shared.js` modules shared verbatim by server and client. Two supporting API routes: `api/demos/webhook-sink` (HMAC-verified receipt sink) and `api/demos/set-tenant` (session tenant switcher). Homepage tiles carry a per-demo `since` badge; demos with persistent user content (`offline`, `tenants`, `flags`, `outbound-webhooks`, `phases`) register with the purge cron.

- **0.6 platform wiring: coordinators, admin plane, tenancy, protocol version.** `src/lib/server/redis.js` now constructs the full 0.6 coordinator set (CRDT cluster, smooth cluster, topic broadcast, alarm store, dead-letter store, retry budget, webhook breaker) inside `activateRedisInfrastructure()`, exposed via `redisCoordinators()` and torn down on shutdown. `src/hooks.ws.js` uses `realtime({ tenant, admin, protocolVersion })` as a config-only seam (the hand-rolled init/cron hooks stay authoritative) and exports `admin`, so the adapter mounts the fail-closed `/__realtime/*` plane (introspect, DLQ inspect + replay, lifeline metrics; bearer `ADMIN_TOKEN`, unset = deny all). Identity sessions gain an optional `tenant` field (`acme` / `globex`). `src/lib/protocol-version.js` pairs `realtime({ protocolVersion })` with the client's `configure({ protocolVersion })`; a tab whose baked bundle is older than the server gets a sticky `outdated` health state and the layout's reload banner. `svelte.config.js` sets `healthCheckPath: false` - the 0.6 adapter default `/healthz` would shadow the app's dependency-aware route - and pins the readiness probe at `/readyz`.

- **Versioned database migrations replace `schema.sql` and runtime DDL.** Ordered, checksummed SQL files under `migrations/` are applied by `scripts/migrate.mjs` (`npm run migrate`) under a Postgres advisory lock, recorded in a `schema_migration` table, and re-verified by checksum on every run. A `migrate` compose service runs them pre-traffic (the `app` service waits on `service_completed_successfully`), and `deploy.sh` applies them before starting replicas. Runtime request/worker startup performs no DDL anymore: the `ALTER TABLE` bootstrap in `hooks.server.js` is gone, and startup only ensures well-known application data exists.

- **Readiness endpoints.** `GET /healthz` returns structured per-dependency readiness (`postgres` including applied-migrations check, `redis`, with bounded probe budgets via `READINESS_TIMEOUT_MS`) as 200/503 JSON; a per-container Unix-socket probe (`$lib/server/local-health` + `scripts/container-healthcheck.mjs`) backs the Docker `HEALTHCHECK` without traversing the public listener. `deploy.sh` and the new `scripts/deployment-smoke.mjs` gate rollouts on it.

- **Redis failure-domain isolation.** New `$lib/server/redis-breakers` supplies separate circuit breakers for the realtime infrastructure and the identity-session store, so session-store failures cannot open the realtime breaker (and vice versa); session reads/writes are bounded by `REDIS_SESSION_TIMEOUT_MS`. `$lib/server/redis-options` bounds command retries, initial-connect attempts, and reconnect backoff so no Redis call can hang a request indefinitely. `$lib/server/redis-command-safety` observes every command promise so extension-owned fire-and-forget commands (timers, reconnect paths) cannot terminate the worker as unhandled rejections.

- **Note-move buffering extracted to `$lib/server/note-buffer`.** The per-note write-behind cache from the boards module now lives in its own unit-tested module with generation-tracked merges: a buffered position always survives a concurrent DB read, a failed flush leaves the entry dirty for retry, and a move that races a flush keeps the newer position. During a Postgres outage, moves on a warm note keep publishing and the last position flushes durably once Postgres returns (covered by the new `e2e/resilience.spec.js`).

- **CI and a unit-test tier.** GitHub Actions workflow runs `npm ci`, `npm audit --audit-level=high`, and `npm test` (unit tests + the safe e2e tier) on Node 22.19. New `tests/unit/` suite covers the advisory lock, note buffer, breakers, Redis options and command safety, e2e target validation, and field validators. Local runners: `npm run test:unit`, tiered e2e via `scripts/run-local-e2e.mjs` (`safe` / `main` / `isolated` / `cluster` / `resilience` / `stress` / `destroyer` / `diagnostics` / `complete`), and `npm run check` (svelte-check + syntax sweep of every shipped .js/.mjs).

- **Deployment pipeline hardening in `deploy.sh`.** Images are tagged with the exact git revision (rollback never relies on a mutable `latest`), the Linux entrypoint is byte-smoke-tested before rollout, migrations run pre-traffic, and a failed rollout automatically rolls replicas back to the previous image (`docker-compose.rollback.yml` disables the image-local healthcheck so older images can restart) and re-verifies the site with an external smoke check (readiness + page + WebSocket upgrade). The certbot service now also verifies the served certificate after each renewal (`scripts/verify-served-cert.sh`); transient renew/verify failures log and retry at the next 12h interval.

- **E2E target safety.** `scripts/test-target.mjs` rejects non-loopback E2E targets unless `ALLOW_REMOTE_E2E=1` is set explicitly, so a stray `BASE_URL` can no longer point the destroyer or stress suites at production by accident. Shared WS RPC helpers moved to `e2e/ws-rpc.js`.

- **Destructive demo controls ask first.** Eleven controls wipe state that every visitor of a public demo shares (`checkout` reset, `collab-editor` clear document, `counter-resume` reset, `effect` clear feeds, `flash-sales` reset sale, `forget` erase, `jobs` clear, `pressure` clear shed log, `schema-evolution` reset, `todos-rollback` clear all, `upload` clear files). Each now routes through `$lib/confirm-destructive`, which appends one shared warning to a caller-supplied question, and each moved from `btn-ghost` to `btn-outline btn-error` so the dress matches the consequence. The e2e suite gained `confirmAndClick` / `dismissConfirmation` helpers that assert the dialog's type and text, so a control that silently loses its gate fails loudly instead of being auto-dismissed into a no-op.

- **`/demos/from-seq` can reach its third tier without a 200-second wait.** The reconnect ladder is replay buffer -> `delta.fromSeq` -> loader rehydrate, but the Redis replay buffer never reports a miss, so `delta.fromSeq` was unreachable in this app and a visitor had nothing to look at. A new "Arm fast fromSeq" control subscribes to a demo-scoped parallel stream whose adapter replay seam deliberately reports a bounded-buffer miss, so a two-second pause exercises the real `delta.fromSeq` bridge. The production stream keeps its unchanged three-tier behavior; the fast stream carries its own durable sequence domain, and its bridge returns a miss (falling through to the loader) whenever the resume cursor lies outside the window its store can actually answer for, rather than returning a confidently wrong slice.

- **`/demos/arena` shows the boundary it is about.** The viewport grew from 640x420 to 900x600 so the 420-unit cull rim and the complete 300-unit LOD ring are both on screen, with both drawn as camera-centred rings plus a legend; the old view sat entirely inside the cull circle, making the demo's defining boundary invisible. Touch devices get a d-pad that feeds the same held-direction set the keyboard uses, so a held button moves continuously and diagonals work.

- **Operational env knobs (all optional).** `DATABASE_POOL_MAX` (default 10), `DATABASE_CONNECT_TIMEOUT_MS` (2000), `REDIS_SESSION_TIMEOUT_MS` (1000), `READINESS_TIMEOUT_MS` (2500), documented in `.env.example`. `.nvmrc` and an `engines` floor pin Node >= 22.19.0; `.gitattributes` normalizes line endings.

- **A wait that clears the connection gate now says what the page asked for.** The connection probe proves a socket opened. Past that point, a wait that times out on the first thing a page renders from live data reported only the selector it gave up on - and the explanations left have different owners: a subscription never sent is the client, one never answered is the server, one refused is authorization, and one answered means the data arrived and the page did not render it. `watchWire` records the RPC traffic from Playwright's side (the stream requests a page sends, the replies that correlate back by id with their topic and row count, and the topics delivering afterwards), and `waitForData` attaches the verdict to the timeout. The board canvas and the flash-sales product cards, the two waits seen failing in this shape, now go through it, each filtered to the one stream it depends on: unfiltered, the report can only say that *some* subscription succeeded, which is equally true of a page showing nothing. Three guards keep it honest about its own reach - a record installed after the socket opened reports `NO EVIDENCE` instead of accusing the page, a filter matching no stream names the streams that were requested rather than presenting a typo as a finding, and a spec that intercepts its own socket is refused outright, since the relay a route handler opens carries the very reply the handler withheld from the page. The verdict and the frame accounting are a pure module (`test/e2e/wire-report.js`) with unit coverage, which is what exercises the two states a browser cannot be driven into without mocking the socket - the arrangement the report declines to speak for. New `test/e2e/_subscription-shape-probe.spec.js` (diagnostics tier) prints the live frame shapes the accounting is built on.

- **A privacy submission that vanishes now says whether the aggregate ever published.** The raw count failing to move is compatible with two faults that have different owners, and the message named neither: the aggregate never published, or it published a value that did not contain the submission. One thing was never in doubt and is worth stating, because it narrows the rest - the page sets `submittedScore` only after `submitMood` resolves, and the helper asserts the resulting note before it starts polling, so by the time the poll fails the server has accepted the write. What was missing was the frame. `submitMood` now marks the raw aggregate's topic before the click and reports what arrived after it: `NEVER PUBLISHED` when no frame reached the topic, or `PUBLISHED` with the payload the last frame carried, which is what separates a reducer that skipped a submission from a page that failed to apply one it was given. The topic is resolved from the server's own reply rather than held as a constant, so a rename fails the test instead of leaving it asserting about a name nothing publishes to; a reconnect that leaves a stale unanswered request in front of the answered one still resolves to the answered one. The report refuses to answer where it cannot: an intercepted socket, a record installed after the socket opened, or a stream whose reply never named a topic, the last of which names the streams that did.

- **A page with no client is now three findings, not one guess.** `PAGE LIKELY NEVER HYDRATED` was one hedged sentence covering three faults with three different fixes, and only one of them is helped by the recovery reload: the entry chunk never arrived, it arrived and the client never ran, or the document came back without a client in it. The wait already watched for failed requests; it now records the entry chunk's *response* too, and a successful one is evidence in its own right. `BUNDLE DELIVERED, CLIENT SILENT` names a 200 that arrived with no socket constructed, which excludes the network and tells the reader a reload will not help. `BUNDLE NEVER REQUESTED` names a document that never asked for a client at all, which is a fault in what was served rather than in the network under it. The proven wording and the reload it gates are untouched, because only proof of a *delivery* failure may trigger a re-fetch. Both new readings are covered by tests that produce the condition rather than describe it - an entry chunk fulfilled as empty JavaScript, and a document fulfilled without one - and each asserts the two verdicts it must not reach. The fallback reads its evidence defensively: this code runs only when something is already broken, and a reporter that throws while reporting replaces the evidence with a stack trace about itself.

### Changed

- **BREAKING (local e2e): every tier now runs the production build, never `vite dev`.** `scripts/run-local-e2e.mjs` builds once and starts `build/index.js` for all tiers; the dev-server path and `startApp`'s `production` parameter are gone. Debugging against the dev server is still possible by running `npm run dev` and pointing Playwright at it through `BASE_URL`. Two reasons. First, the dev server intermittently failed to hydrate a page: the SSR HTML rendered, Vite's own HMR socket connected, and the app bundle never booted, leaving the page with no app socket, no pending retry, and a connection status still on its initial value - so it never recovered at any timeout, and the gate reported a `waitForWS` failure against a page whose JavaScript never ran. Measured at roughly one open in 900 while cycling routes, and zero in 80 opens of a single warm route, which is why re-running a failed spec in isolation always "passed" and cleared code that was never at fault. Second, and larger: the demo serves WebSockets through `svelte-adapter-uws/vite` in dev and through the adapter's production handler when built, so every tier except `stress` and `destroyer` had been validating a WebSocket implementation that is never deployed.

- **The local e2e harness reclaims containers left by runs that were killed.** `scripts/run-local-e2e.mjs` removes its Postgres and Redis containers in `cleanup()`, which covers a normal exit and SIGINT/SIGTERM but not a harder kill, and nothing else ever reclaimed them: eight were found still running, the oldest 29 hours old, alongside 677 dangling volumes holding about 16GB. The sweep now runs at STARTUP, the only placement that survives a kill, and keys on the creating PID that the container name already carries, so a container whose owner is gone cannot have a live one. A recycled PID reads as alive and its container is left alone, because concurrent harness runs are legitimate and deleting a running tier's database is far worse than leaving a leak in place. Container removal also passes `--volumes` now, so the anonymous volume goes with the container instead of accumulating invisibly. Selection logic lives in `scripts/orphan-sweep.mjs` with unit coverage, since `run-local-e2e.mjs` runs its whole pipeline on import and anything left inside it is reachable only by running a full tier.

- **BREAKING (local scripts): lean-root restructure.** The eight root shell scripts moved to `scripts/` (`deploy.sh`, `entrypoint.sh`, `init-app-role.sh`, `init-certs.sh`, `renew-certs.sh`, `reset-all.sh`, `reset-db.sh`, `reset-redis.sh`; each now `cd`s to the repo root itself), and the test trees moved to `test/e2e` (was `e2e/`) and `test/unit` (was `tests/unit`). Anything invoking the old paths - shell history, local aliases, external runbooks - needs the new prefix. Playwright config, unit runner, `jsconfig.check.json`, `.dockerignore`, the Dockerfiles, and the README all reference the new layout.

- **The demo gallery chrome learned the new demos.** The homepage catalog and the `/demos` sidebar register all 34 demos; tiles show a per-demo `since` version badge. `/demos/pressure` renders the 0.6 pressure payload (PSI `cpuSome10`, cgroup `throttledRatio`, `maxBufferedBytes`, `backpressuredConnections`) alongside the scalar. The topic registry (`src/lib/server/topics.js`) covers every new demo topic and the new modules reference it, keeping it the single source of truth for the wire namespace.

- **Bumped the realtime stack to the 0.6.0-next lines: `svelte-realtime` `^0.6.0-next.88`, `svelte-adapter-uws` `^0.6.0-next.75`, `svelte-adapter-uws-extensions` `^0.6.0-next.52`.** These tips carry the fixes a full demo e2e pass surfaced across several rounds. The first round cleared the build blocker and SSR crashes: the adapter crdt client codec no longer drags server `node:` builtins into the browser bundle, realtime's SSR stubs gained the `live.flag` set-merge readable and the multiplayer `room().bindDoc` method, and extensions' `presence.purgeUser` no longer throws on a topic-level observer refcount (unblocks `live.forget`). The later `.75`/`.88` tips fix runtime races a full pass then surfaced: the adapter client reacts to browser `online`/`offline` events so the offline-queue demo arms without waiting for a ping timeout; `live.smooth({ onTick })` arms its tick on a bare subscribe so server-driven entities animate for a watching-but-idle client (`arena` / `shooter`); a `live.room({ owner: true })` first joiner reliably receives the ownership its own join claimed; and room presence plus enumerable counts now converge and release across replicas on a live-socket unsubscribe. Because this app owns the adapter `unsubscribe` hook, it now chains realtime's managed-topic drain before the extensions presence cleanup; the lobbies cluster regression is a normal green assertion again. The 0.6 extensions stamp `user_id` (and `tenant_id` on idempotency rows) into the durable jobs stores so a user's rows can be purged; the demo's `demos_jobs_*` tables run `autoMigrate:false`, so migration `003_demo_jobs_erasure_columns.sql` carries the same forward-only ALTERs.

- **The `/demos` rail now appears at 1024px instead of 640px.** A fixed 13rem sidebar from 640px up meant every child page's `sm:` and `md:` breakpoint described a width the content column did not have, so responsive rules fired against a lie. Keeping the 640-1023px band rail-free makes those breakpoints honest; the switcher renders as a horizontal strip below 1024 and the active entry is scrolled into view within the nav's own scroll container (never the document). Child pages whose three-up rows fired exactly where the rail returns moved out to the next band. Sidebar titles were also reworded so the entry, not the primitive, leads.

- **`/demos/upload` describes `live.notify` accurately.** The page and module claimed the upload notification fanned out to "every other tab the same user has open". `live.notify({ userId }, ...)` targets the user's most recently connected socket, locally and through the cluster registry - last-write-wins, not fan-out. Copy, headings, and the closing explainer now say that. Re-uploading a file is also an upsert rather than a second logical entry, since ids are content-addressed: the record, its order-list position, and the byte counter move together in one Redis turn.

- **`editNote` accepts only `content` and `color`; position fields moved exclusively to `moveNote` / `focusNote`.** The note buffer owns x/y during a drag (a buffered position always wins over a DB read), so an `editNote` position write would be published and then reverted by the next flush. `validateNoteFields` drops `x` / `y` / `z_index` from the edit allowlist; a position-only `editNote` payload now returns a VALIDATION error. All in-repo callers already used `moveNote` / `focusNote`.

- **`npm run build` wraps vite via `scripts/build.mjs`.** SvelteKit's post-build analysis imports server chunks under `NODE_ENV=production`, which would trip the fail-closed webhook-secret check on machines without the real secret; the wrapper supplies an analysis-only placeholder while production startup still reads its own environment. `svelte-check` runs against a dedicated `jsconfig.check.json`.

- **`/demos/pressure` labels its publish-rate source.** Production reads the adapter's pressure snapshot as before; `vite dev` (where the dev platform reports a zero snapshot) falls back to a demo-side generated-load counter and the payload says which source produced the number, so the readout is never silently wrong in dev.

### Security

- **`npm audit` reports zero vulnerabilities: `@sveltejs/kit` 2.60.1 to 2.70.2, `vite` 7.3.5 to 7.3.6, and four transitive advisories closed with it.** The kit bump clears the two remote-function advisories (neither reachable here - no `*.remote.js` module exists, nothing imports `$app/server`, and the feature is off by default with no `experimental` block to turn it on), and the vite bump widens the esbuild range so `esbuild` moves 0.27.7 to 0.28.2, closing the dev-server file-read advisory. `cookie` needed an `overrides` entry rather than riding along: kit 2.70.2 still declares `cookie: ^0.6.0`, which cannot resolve 0.7.0, so the assumption that the kit bump carries it is wrong and the lock is pinned to 0.7.2 explicitly. Three further high-severity advisories that no alert had been filed for - `undici` (8.5.0 to 8.10.0, a direct dependency), `postcss` (8.5.14 to 8.5.26) and `nanoid` (3.3.12 to 3.3.18) - are closed in the same pass, because `npm audit --audit-level=high` is a CI step and leaving them would keep that step red on a repo whose alerts had just been declared triaged.

- **A WebSocket handshake that carries no `Origin` is refused where a canonical origin is configured.** The adapter compares a handshake's `Origin` against the deployment's `ORIGIN` and answers a mismatch with 403, but a handshake with no `Origin` at all is a different case: its check returns "allowed" for that whenever an `upgrade` hook is exported, on the grounds that the application will authenticate the client itself. Exporting a hook therefore turned every non-browser client into an unconditional admit, and this one only validated the identity cookie's shape. The hook now refuses an Origin-less handshake (401, the status the adapter gives a hook rejection) and logs the refusal with its source address. The policy keys on `ORIGIN` being set rather than on `NODE_ENV`, because the production build is run with `NODE_ENV=production` in places that are not public deployments - the local e2e tier builds and runs exactly that way - so `NODE_ENV` does not separate a deployment from a laptop and a configured origin does. A checkout with no `ORIGIN` keeps the previous behaviour, so a fresh clone still runs with no configuration. `WS_ALLOW_ORIGINLESS=1` re-admits them for a deployment that genuinely fronts non-browser clients. Browsers always send `Origin` on a handshake, so no visitor loses a connection; the load generators under `test/e2e` now send one derived from the URL they were pointed at, which keeps a single generator correct against a local instance and against a deployment without being told which it is addressing.

- **Responses carry `Content-Security-Policy: frame-ancestors 'self'`, and a request whose `Host` is neither the canonical name nor a loopback name is refused with 400.** The framing directive is the half that stands on its own: an origin comparison can say nothing about a page embedded in a frame, because the socket such a page opens is same-origin by construction and correct on every check. Only the framing directive addresses that shape. The `Host` check is the weaker half and is worth being plain about - anything sitting in front of the server rewrites `Host` trivially, and the common proxy default already sends the upstream's own name, which passes unchanged. What it catches is a front end that forwards the client's `Host` verbatim. Loopback names are always accepted so container health probes, the local e2e tier and `npm run preview` address the server directly as before, and both checks are inert until `ORIGIN` is set.

- **`ALLOWED_ORIGINS` (build-time) admits a deployment reachable under more than one hostname.** Comma-separated full origins, passed through to the adapter's origin policy; unset leaves it on `same-origin` pinned to the runtime `ORIGIN`. It is read when the bundle is built rather than when a container starts, because the origin policy is serialized into the build, so the compose file carries it as a build argument. New `test/e2e/ws-admission.spec.js` drives the handshake from a node client, which is the only way to set an `Origin` a browser will not let script touch, and asserts the accepted case alongside the refused ones - a suite that asserts only refusals passes equally well against a server that refuses everything, including one that is simply down.

### Changed

- **Removed `websocket.compression` from `svelte.config.js` (was `1` = SHARED_COMPRESSOR; now omitted, reverts to uWS default `0` = DISABLED).** Empirical: enabling permessage-deflate at 1000-cursor stress on Hetzner CCX13 saturated both modes -- SHARED_COMPRESSOR=1 because all 1000 subscribers serialized through one deflate context, and DEDICATED_COMPRESSOR_3KB=9 because per-connection contexts at scale fan-out to ~1000 simultaneous compress operations per cycle. In both cases, TLS handshake latency climbed from ~100ms to 8+ seconds during sustained load, and the e2e harness declared the demo unresponsive across 7 of 8 shards. The bandwidth win compression promises is not worth the CPU cost on a 2-vCPU box at this connection count. Wire-byte reduction at this scale belongs at the wire-format layer, not in deflate.

- **Bumped `svelte-adapter-uws` from `^0.5.6` to `^0.5.8` and `svelte-adapter-uws-extensions` from `^0.5.7` to `^0.5.9`.** Two coordinated upstream changes consumed in this bump. **(1) Presence diff + relay batching via `setTimeout(0)` (adapter 0.5.8, extensions 0.5.9).** Same bug class the 0.5.6 cursor always-tick rewrite fixed -- `bufferDiff` in `redis/presence.js` and the cross-worker relay scheduler in `redis/pubsub.js` / `redis/sharded-pubsub.js` previously deferred their batched flush via `queueMicrotask`, which drains between WS dispatches in uWS's per-socket task model. Mass-join into a populated presence topic produced O(N) one-entry diffs instead of one batched per topic; the demo's cap probe at 8000 ws measured ~800 KB/conn outbound buffer accumulation vs the steady-state ~40 KB/conn linear curve. `setTimeout(0)` lands in libuv's timers phase after the poll phase has dispatched every ready socket message, so all co-arriving joins coalesce into one diff per topic. **(2) BREAKING: presence wire event names renamed `presence_state` -> `state`, `presence_diff` -> `diff` (adapter 0.5.7, extensions 0.5.8).** The `__presence:` topic prefix already namespaces the channel, so the redundant `presence_` event-name prefix was dropped to match the wire shape of cursor (bare `catalog`/`update`/`bulk`/...), groups, and replay. **Breaking only for hand-rolled wire decoders;** the bundled `presence()` Svelte store (which the demo uses for both `presence('global')` and `presence('board:UUID')`) updates in lockstep, so the demo's callers are unaffected. Prometheus metric names (`presence_diff_frames_total`, `presence_diff_coalesced_total`) are unchanged -- they are Prometheus-side identifiers, not wire events. See respective package CHANGELOGs for full mechanics.

- **Bumped `svelte-adapter-uws` from `^0.5.5` to `^0.5.6` and `svelte-adapter-uws-extensions` from `^0.5.6` to `^0.5.7`.** Supersedes the 0.5.5 / 0.5.6 `queueMicrotask` defer for cursor leading-edge fragmentation. The microtask defer assumed co-arriving broadcasts share a JS task -- they don't: uWS dispatches each WS message as its own task and microtasks drain between dispatches, so the deferred flush fired BEFORE the next socket's handler ran. Demo verification at 1000-mover load on Hetzner CCX13 measured ~99% UPDATE / ~1% BULK under 0.5.5+0.5.6, essentially identical to the pre-fix shape -- per-cursor effective rate still ~0.14 Hz against the 125 Hz target. The always-tick rewrite drops the leading-edge sync fire AND the microtask defer; every `broadcast()` (and `enqueueInbound()` in the cluster path) queues into `dirtyTopics` and arms the tracker-wide `tickTimer`. The `setTimeout(0)` arm fires after libuv's poll phase processes every ready message on every socket, so flushBoth sees the fully-accumulated dirty map from all source sockets in the same event-loop iteration. Cost: up to `topicThrottleMs` of latency for the first message on an idle topic (acceptable at 8-16 ms cursor cadence); benefit: structurally correct independent of dispatch model. Both fixes ship together because extensions 0.5.7 requires adapter `^0.5.6`. See respective package CHANGELOGs for full mechanics.

- **Bumped `svelte-adapter-uws` from `^0.5.4` to `^0.5.5`.** Two production-stress fixes filed against the demo during the 1000-cursor smoothness investigation. (1) **`platform.*` closed-WS race.** `platform.subscribe`, `unsubscribe`, `send`, `sendCoalesced`, `sendTo`, and `request` previously threw uWS's `"Invalid access of closed uWS.WebSocket"` when the underlying socket closed during an awaited gate (e.g. the user-subscribe hook chain). Uncaught -> worker exit -> Docker restart -> hundreds of subscribers dropped per cascade. Adapter now swallows the throw, returns a success-shaped no-op sentinel, and bumps a new `platform.closedWsAborts` counter so operators can detect drop frequency. The demo's stress run was crashing `app-3` deterministically under mass-connect on the 2-replica deploy; this fix eliminates the cascade. (2) **Cursor `topicThrottle` leading-edge fragmentation.** `broadcast()` in `plugins/cursor/server.js` now defers the leading-edge flush by `queueMicrotask`, so cursors co-arriving in the same JS pass batch into one bulk frame instead of fragmenting into one single-cursor UPDATE + N-1 trailing entries. The demo's smoothness probe pre-fix measured 1794 UPDATE / 38 BULK (86% fragmentation) over 30s at 1000-mover load; per-cursor effective rate collapsed to 0.14 Hz against a 125 Hz target. Adapter microbench (100 bursts x 250 cursors) post-fix: 0 UPDATEs / 100 BULKs of exactly 250 entries each. See `svelte-adapter-uws` CHANGELOG for full mechanics.

- **Bumped `svelte-adapter-uws-extensions` from `^0.5.4` to `^0.5.6`.** Two coordinated fixes mirroring the adapter 0.5.5 work. (1) **`presence.join` + `cursor.attach` throw `WsClosedError` instead of silently undoing.** Pre-fix, both functions detected a closed WS after `await subscribeToTopic` (via the `getBufferedAmount` probe) and returned silently after state rollback; the calling `live()` wrapper saw no error and reported the RPC as `status="ok"`. Under mass-connect, 10-15% of `boards/cursors/joinBoard` RPCs were silently dropped this way -- the metric lied. Now throws `WsClosedError` (stable code `'WS_CLOSED'`, re-exported from both modules) so the RPC metric labels these as errors and callers (including `PresenceBar.svelte`'s retry path) can branch on the cause. Adds `presence_joins_aborted_total{topic,reason}` and `cursor_attaches_aborted_total{topic,reason}` for fleet visibility. (2) **`redis/cursor.js` mirrors the adapter's microtask-deferred leading-edge** for the cluster path, including `enqueueInbound()` for peer-relayed cursors. `cursor.attach` switches to raw `ws.subscribe` so the WsClosedError throw contract survives adapter 0.5.5 swallowing uWS throws at the platform layer (without this switch, both branches return null and the caller can't distinguish closed-WS from success). Peer dep floor bumped to `svelte-adapter-uws ^0.5.5`. See `svelte-adapter-uws-extensions` CHANGELOG for full mechanics.

- **Bumped `svelte-adapter-uws` from `^0.5.3` to `^0.5.4`.** Cursor flush scheduler rebuilt: per-topic `setTimeout` (which compounded drift under load because `lastFlush` was anchored on actual fire time, pushing every subsequent cycle's deadline) replaced with one tracker-wide `tickTimer` aimed at the next earliest topic deadline; `lastFlush` now advances by `topicThrottleMs` (target-anchored), so a late fire affects exactly one cycle and a multi-cycle backlog under saturation collapses to `now` instead of producing phantom catch-up fires. The demo's smoothness probe measured per-worker fan-out at ~50Hz against a `topicThrottle: 8` (125Hz) target on the CPX22 deploy pre-fix; the target-anchored scheduler restores intended cadence under saturating incoming RPC load. Adds a `stats()` accessor on the cursor tracker returning `{flushes, driftMeanMs, driftMaxMs, dirtyTopicsCurrent, activeTopicsTotal}` -- always-on, near-zero cost -- so operators can detect sustained event-loop saturation (`driftMeanMs > topicThrottle`) or GC pauses (`driftMaxMs` spikes vs. mean) from a single call. Also soft-flips the presence server `heartbeat` default from `0` (disabled) to `30000` (30s) and the client `maxAge` default to `90000` (3x safety); this demo already sets both explicitly (`src/lib/server/redis.js` and the presence-store callers), so no behavior change here. See `svelte-adapter-uws` CHANGELOG for full mechanics.

- **Bumped `svelte-adapter-uws-extensions` from `^0.5.3` to `^0.5.4`.** Cluster cursor smoothness fix. Pre-fix, each uWS worker emitted both its own local cursor flush AND immediately republished peer-relayed cursors (received via the Redis pub/sub channel) as a separate frame, so any single subscriber saw tight doublets per cycle. The demo's smoothness probe at `e2e/_smoothness-probe.spec.js` measured `p50 = 0ms` inter-frame intervals with 38% of those tight (<3ms) pairs carrying keys from a different worker's `instanceId` than the previous frame -- the cross-replica double-emit pattern, observable as jitter on 120Hz displays and doubling outbound bytes per subscriber. Post-fix, the Redis subscriber handler in `redis/cursor.js` routes inbound `update` / `bulk` frames into a per-topic `inboundDirty` map; the next local flush emits one combined frame covering local + peer cursors. The relay payload remains the local-origin slice only -- re-relaying received cursors would loop (filtered at receiver, but wastes pub/sub bandwidth `O(worker_count)`) and would double the `queueSnapshot` HSET storm (origin owns the HSET). `catalog` / `join` / `remove` bypass aggregation: low-frequency roster events where latency matters more than smoothness. Mirrors the matching scheduler rebuild + `stats()` accessor from the adapter (above). Rolling-deploy safe: old worker still emits the legacy doublet pattern for subscribers on new workers; full benefit when every worker is on `^0.5.4`. See `svelte-adapter-uws-extensions` CHANGELOG for full mechanics.

- **Bumped `svelte-realtime` from `^0.5.3` to `^0.5.4`.** 0.5.4 closes the dev-mode publish-rate-sampler retention leak via WeakRef + per-process epoch; sampler self-clears when its captured platform is GC'd or when reset / disable bumps the epoch. The demo's `docker-compose.yml` already sets `NODE_ENV=production` so the dev path is dead in this deployment regardless, but the framework fix means a future deploy with a missing `NODE_ENV` (or anyone running locally in dev) no longer leaks. See the framework CHANGELOG for full mechanics.

### Fixed

- **The schema-evolution page shows what the migration does, and a phone sees the demo instead of a wall of prose.** Both panels render identical rows - same labels, same values, same dot colours - so the only visible difference was a badge, and the visitor was asked to verify that a migration ran without ever seeing what it transformed. The page now shows a v1-shaped row beside the row `migrate[1]` turns it into, produced by the real `v1ToV2` on the server rather than written out by hand, so it cannot drift from the code it documents. And at 320-360 the first screen held the heading and seven lines of inline-code-heavy prose and nothing else - no panel, no counter, no button - so the page failed the glance test on the harshest rungs; the intro is clamped to three lines there, with the rest one tap away, and a live counter now crests the fold. Pinned as exactly that: without the clamp the first counter sits 175px below it.

- **The demo switcher stays reachable on a phone.** Below 1024 it is a single compact row in normal flow at the top of the page, and demo pages run 1300-1800px tall - so after one screen of reading it was gone, and "hop to any other demo in one click at any width" meant scroll-to-top first. It is sticky now. That needed `overflow-x: clip` on the shell in place of `hidden`: `hidden` computes the other axis to `auto`, which makes the shell a scroll container, and a sticky child sticks to its nearest scrolling ancestor - so the switcher would have stuck to a box that scrolls with the document and gone off screen anyway. `clip` cuts the overflow without creating that container.

- **The offline composer's primary action meets the touch floor.** daisyUI sizes a default `btn` at 2.5rem = 40px, under the 44pt minimum the rest of the suite holds via `pointer-coarse:min-h-11`. The toggle beside it had already been raised, so the two controls of the demo's scripted flow disagreed about the standard - with the primary action being the one that missed it.

- **The two schema-evolution panels put the same counter at the same height, and the page stops asserting a server version nobody sent.** The panels are an equal-height grid and daisyUI gives `card-body`'s paragraph flex-grow, so descriptions stretched and bottom-anchored the rows. Measuring showed the description was not the whole cause: at the tablet rung the right panel's title wraps to two lines and the left's does not, so the rows were offset by the heading as well. Both panels are subgrids of the section now, which aligns title, description and rows across the pair at every rung the two columns exist at. Separately, `onMount` swallowed a failed state call, leaving a permanently empty code box and a version chip still showing the *client's* default as though the server had confirmed it. The card now distinguishes three states - loaded, still waiting, failed - and the chip appears only once real state arrives. Forced in the test by dropping the call on its way out, since the reply carries a correlation id rather than the handler name and filtering the response would have matched nothing.

- **The checkout burst is visible as a burst.** `Promise.all` held all five results until the last one returned, so five rapid RPCs landed on screen as a single atomic `+1` - the copy promises five events and the visitor saw one, with nothing but a dimmed button in between, which on a slow link runs past the second where a page starts reading as broken. Each call now enters the history the moment *it* settles, and the button reports how many of the five have, so a burst still in flight says so. The pin slows the server's frames through `routeWebSocket`, because on localhost every reply settles within a millisecond or two and a bare click would sample the finished state - a pin on nothing.

- **The flash-sales coupon claim is one atomic step, so no crash can leave a holder against a pool that never moved.** Gating on `SADD` made the test-and-set atomic and let exactly one caller in the cluster reach the decrement, but the decrement was a *second* command - and the gap between them is a crash window: a worker dying after the add and before the decrement records a holder who took a coupon nobody paid for, with no compensating write left to run, and the exhaustion rollback had the same shape in reverse. The add, the decrement, the seed and the rollback are now one Redis script, which has no gap to die in. It returns its outcome (claimed / already a holder / exhausted) rather than letting the caller infer it from the pool value, because "already a holder" and "just claimed" can report the same number.

- **`/demos/todos-rollback` is usable, readable, and observable on a phone.** Four findings on one page. The add form could not wrap, so at 320px the input collapsed to roughly 70px with its placeholder truncated mid-word - the act every step of the page begins with happened in the smallest target on screen; the row wraps now, input on its own line with the buttons beneath. Five identical FORCED failures stacked five toasts, which on a phone rung cover the lower half of the viewport including the Todos card, during exactly the seconds the placeholders vanish - so the feedback destroyed the view it was reporting on; identical failures now coalesce into one counted row (`5x FORCED - ...`), keyed on the cause rather than the per-call label, and the alert is clamped to the viewport instead of a 448px max-width no phone can hold. Under force-fail the server threw immediately, so a placeholder's whole on-screen life was one fast round trip and the demo's central observable was never actually observed - the artificial rejection is held 400ms, which is the artificial half of an artificial failure and costs the honest path nothing. And the row checkbox and remove glyph had no accessible name at all, so a screen reader announced unlabelled controls; both name their todo. Each pin was confirmed by mutation: with the delay back at 0 the placeholder is not merely brief but uncatchable - the row count reads 0 where 1 is required.

- **`/demos/checkout` promises what it demonstrates, points at the control that demonstrates it, and stops rebuilding its history.** Three findings on one page. The title said "under double-click" while `busy` disabled both buttons, so a literal double-click was absorbed by the page and never reached the server - what a visitor could actually perform was defended by UI lockout, not by the primitive, and only the scripted burst exercised same-key dedupe. It now reads "under a retry storm" and names the lockout outright, so the system image is honest in both directions. The intro instructs "Retry x5", but Place Order was the visual primary and came first, making the click magnet the *control* case - which increments the counter and shows nothing about idempotency; Retry leads now and carries the primary style, with Place Order secondary. And the history's each-key embedded the array index, so every prepend re-keyed every existing row and Svelte destroyed and rebuilt the whole list; it keys on content alone, which is already unique within one list. The pin for that last one stamps the existing rows and requires the same nodes afterwards, because a rebuilt list is indistinguishable from a preserved one by count or by text - with the index restored, 0 of 5 rows survive.

- **The chat lobby's room cards look like doors on touch, not only under a mouse.** The only interactivity cue was a hover background shift, and hover never fires on a touch device - so a first-time phone visitor could read the lobby as a static list of room descriptions and stall at the page's single required action. Each card carries a trailing arrow inside the link, asserted on a coarse-pointer context, which is the modality where the old cue was definitionally absent.

- **The demo switcher is grouped, and the narrow one says which way it continues.** Thirty-four entries sat under a single faded "DEMOS" label in an order that is pedagogical rather than alphabetical, with labels mixing concepts ("Idempotency"), API names ("live.effect") and products ("Ops dashboard") - so finding a demo was a linear read of up to 34 rows whose ordering principle was invisible, spanning four scroll-screens on a phone. Six group labels now name the districts that order already had; every group is a contiguous run of it, so nothing moved, which is the part the spec pins. Below 1024 the switcher is a horizontal strip several viewports wide with no scrollbar, where the only hint that more existed was whether a label happened to be cut mid-word at the edge - an accident of viewport width against label widths, so a phone visitor could read three items as the whole catalog. The strip now measures its own scroll position and fades the edge that continues, on both sides, and stops fading at the end rather than promising more forever.

- **Both tenants specs share one connection wait, and the cluster one stops waiting blind.** The tenant readout is confirmed over the socket, so waiting on `tn-ws-pending` is a connection wait in app clothing - and the two specs had drifted: the single-instance one routed it through `waitForWS` while the cluster one waited on the marker directly, on both the open and the post-switch path. The post-switch case was the worse of the two, because switching tenant RELOADS (the resolver runs once per connection at upgrade), so it was a wait on a brand-new page's first socket with no diagnostics at all, sitting behind a `tn-active-tenant` check that comes back with the server-rendered document and says nothing about whether the client booted. Both specs now import the same helper from `tenants-helpers.js`, so they cannot drift apart again. The diagnostics were confirmed to fire on this route rather than assumed: forcing the wait to fail there produces the connection-probe block with its status timeline and socket list, which is the report the next real occurrence will carry.

- **`/demos/schema-evolution` takes its counter value and its timestamp from one observation.** The handler read `Date.now()` and then awaited `HINCRBY`, so the pair it published came from two independent orderings: a replica that read its clock early and landed late published the HIGHER value under the EARLIER time. Across replicas it is worse than a reordering, because two workers' wall clocks are not the same clock and the stamps are not comparable at all. Both halves now come from Redis in one atomic script - `TIME` read between the increment and the write - so every timestamp on the page comes from a single clock however many replicas serve it. The reset stamps all three counters from that same clock.

- **The schema-evolution cluster spec asserts the guarantee that holds, and says why the other one is not asserted.** It made one claim out of two - that concurrent increments on separate replicas are atomic AND that both live clients then display the true value - so a red said nothing about which had broken, though they are different faults with different owners. Atomicity is now read through the loader (a reload against shared Redis, which answers what the store holds rather than which frame arrived last) and stays a gate. The display claim is not asserted: each replica publishes the value IT observed as an absolute row and the stream merges by key on arrival, so a client receiving 2 then 1 shows 1 permanently. Measured at 4 failures in 5 consecutive runs. The wire capture settles the mechanism - both frames arrive at both clients in opposite orders, so it is an ordering fault and not a lost publish - and the frames carry nothing a client could order by: `seq` numbers per-subscriber delivery, so the same logical event is seq 5 on one client and seq 4 on the other. It is not recorded as an expected failure either, because at 4-in-5 that reports "passed unexpectedly" on roughly every fifth run, which is a coin flip in the gate rather than a pin; the rate lives in the ordering probe instead.

- **Resetting `/demos/flash-sales` really does permit a fresh coupon claim.** The claim is wrapped in `live.idempotent` keyed on the visitor, and `resetSale` restored the pool and emptied the holders set while leaving that cache untouched - so a visitor who had already claimed got their first reply replayed with the handler never running: no `SADD`, no `DECR`, and a pool sitting at exactly where the reset left it. It read as a pass for as long as the page displayed the number the claim RPC returned, because a replayed reply carries the original one; the page displays what Redis holds, so the discrepancy is a failing assertion instead of a masked one. The claim now takes the same cluster-shared Redis store the checkout, chat and offline demos already use, under a `keyPrefix` this demo owns outright, and the reset empties it - last, after the pipeline, so a claim arriving mid-reset lands on the already-restored pool and only loses a cache entry it no longer needs. The store is also what the primitive needed on a cluster in the first place: the in-process default is per-replica, so a retry landing on another worker would cold-miss and claim twice. The spec pins both directions now - a reset permits exactly one more claim, and that claim is still deduped afterwards, so a reset cannot trade a stale cache for no cache at all.

- **Two concurrent e2e runs no longer corrupt each other's trace artifacts.** Playwright streams trace resources into `<outputDir>/.playwright-artifacts-N` and zips them when a context closes, so two runs sharing `test-results/` share that staging area - and one run's startup cleaning removes files the other is still writing. It surfaces at `context.close()` as `ENOENT ... traces/resources/<hash>.css` followed by "not a zip file, or file is truncated", on a test whose assertions all passed, and it names a file nobody has heard of; a full tier reported it as a failure of whichever spec happened to be closing a context. Each run now writes to `test-results/srd-build-<pid>-<timestamp>/` (new `PLAYWRIGHT_OUTPUT_DIR`, default unchanged for an ad-hoc `npx playwright test`), swept on a clean run and kept on a failing one so the evidence survives, with the same owning-pid sweep the containers and build directories use.

- **A concurrent build can no longer void a live e2e tier, or corrupt the build it is racing.** The harness gave every run its own containers but shared one `build/` directory, and Node ESM lazy-loads a route's server chunk on the *first* request to that route - so the on-disk tree is a live dependency of the running server for the whole tier, not just at boot. A second build in the checkout rewrote the chunk filenames the running server's manifest already pointed at; warm routes kept passing out of the module cache while the next cold route died with `ERR_MODULE_NOT_FOUND`, and everything after that ran against a half-replaced build with nothing in the output saying so. Each run now builds into `build-runs/srd-build-<pid>-<timestamp>/` (new `BUILD_OUT_DIR`, default `build`) and serves from there, so the served tree is immutable for the run's lifetime. Ownership is the pid, the convention the containers already used, so a directory left by a killed run is reclaimed by the startup sweep on the same evidence its containers are - the one placement that survives a hard kill. Builds also take a lock (`node_modules/.cache/srd-build.lock`) so two of them serialise instead of racing through the shared `.svelte-kit` and Vite caches: that collision surfaced as `failed to load virtual css module` for an untouched component, whose raw source was then handed to Tailwind and its leading HTML comment parsed as a CSS declaration - a message that sends the reader looking for a corrupt component there is nothing wrong with. A lock whose holder is gone is reclaimed automatically, so a killed build cannot wedge the checkout.

- **The ops dashboard withholds every unsampled reading, not just the one that was impossible.** The adapter's transport pressure snapshot exists from process start with all fields at `0` and is overwritten when its ~1Hz sampler folds, so `/demos/ops` opened inside that window rendered placeholders as measurements. Only RSS was defended, by a rule that no live process weighs 0 MB - true, and unreachable for `publishRate`, `subscriberRatio`, `maxBufferedBytes` and `backpressuredConnections`, whose zero is the honest reading on an idle worker. The panel now branches on `pressure.sampledAt`, which dates the snapshot instead of guessing at it, and covers all of them at once: undated means the whole panel says so, and reports the same state under `npm run dev`, where no sampler runs at all. Because it is a timestamp it also separates readings that are merely old from readings that were never taken - a sampler that stops leaves real numbers behind, which is neither "no data" nor healthy - so the panel dates every reading it shows and says outright when the sampler has stopped folding. The age is computed on the worker that took the sample: `sampledAt` is that worker's wall clock, and subtracting it from a visitor's clock would report the offset between two machines as the age of a measurement. The assertion that a *reported* RSS is never 0 stays, now as the check that catches a placeholder reaching the page by some route other than the one that was closed.

- **`waitForBoardReady` is a gate that can fail.** Around sixty call sites across twelve specs opened a board and then called a helper that could not report anything: both of its waits ended in `.catch(() => {})`, so a timeout was swallowed and it returned as though it had succeeded; the element it waited on was the `h1`, which is server-rendered and says nothing about whether the client ever booted; and what remained was an unconditional 500ms sleep. A page whose bundle never ran passed through in half a second, and the failure surfaced thirty seconds later on whatever locator the test touched first - which is how a real tier failure once read as a canvas selector timeout in `createNote`. It now waits on the connection itself (inheriting the probe diagnostics, so a dead bundle is named rather than inferred) and then on the client-rendered canvas, unswallowed. The transient loading spinner stays best-effort on purpose: a fast board may never show one, so its absence is not a failure. Proven load-bearing by pointing the canvas wait at a selector that never appears - the board specs then fail inside the helper rather than downstream.

- **The collab-editor's own CRDT selection row stays glued, like everyone else's.** The CRDT panel's whole claim is that a published range survives edits in front of it, and it held for every tab except the one that made the selection. `localSelections` stored the raw `{ start, end }` captured at publish time and the local row rendered straight from them, while every remote tab resolved the same selection through its anchor on each edit - so after a remote prefix edit the acting visitor's own row slid onto the wrong characters and the proof panel contradicted itself for the person most likely to be reading it. The local CRDT row now keeps the anchor (`body.anchorRange`) and re-resolves it (`body.resolveRange`) on every render, which is exactly what the room does for peers. The offset panel's local row still renders frozen numbers on purpose: that drift is the thing it exists to show. Note that `room.selections` could not supply this - it filters the local member out by design, so the anchor had to be retained page-side.

- **`/demos/flash-sales` no longer reports a coupon pool nobody measured after a reset.** Splitting the coupon test surfaced that `resetSale` clears the holders set but not the `live.idempotent` cache, so a repeat claimant gets their first response replayed without the handler running - no decrement, no publish, and a pool that stays where the reset left it. That was previously invisible because the page displayed the number the RPC returned, which on a replayed call is the cached one; publishing the pool made the page show what Redis actually holds. The expectation is now its own test carrying a `test.fail()` guard that spells out the mechanism, so the suite is honest about the defect instead of asserting the cached value as correct.


- **The collab-editor intro's panel naming is now pinned by a test.** The copy already named the Offset and CRDT panels rather than placing them left and right, which is what makes it survive the sub-`lg` layout where the panels stack and a position refers to nothing - but nothing asserted it, so the positional wording could return unnoticed. The guard is in two halves because only one of them defends the change: asserting both panel names appear would still pass with "the left room" restored *alongside* them, so the assertion that actually fails is the absence of positional words in the intro. Confirmed by restoring the old sentence next to the names, which the absence half rejects and the naming half does not.

- **`/demos/flash-sales` publishes the coupon pool instead of only returning it.** Two tabs on one identity claiming on separate replicas left the pool reading 48 where one claim had happened. The displayed number came straight from the claim's own reply, and a reply reaches only its caller, so a claim performed on the other replica had no way to correct the tab that did not make it. The pool is now a `merge: 'set'` stream, published by whichever replica actually performed the decrement, which is the only participant that knows the answer. Establishing that was the point of the exercise: removing the publish fails the cluster spec eight times out of eight, with the other tab reading `50` because its duplicate claim's `GET` races the winner's `DECR`. The claim also gates on `SADD` now, which is itself an atomic test-and-set, so only one caller can ever reach the decrement; the previous order read membership first and let both replicas decrement, leaving the pool transiently short and relying on the loser's compensating `INCR` to restore it, so a worker dying in that window retires a coupon nobody holds. That second change closes a durability window rather than a display one, and no test in the suite distinguishes it - with the publish in place the spec passes either way.

- **`npm run check` is green again.** The deployment smoke test's upgrade handler read `.port` straight off `server.address()`, whose type is `string | AddressInfo | null` because a Unix-socket listener reports a path rather than a host and port. `svelte-check` rejected it, and because `check` runs over the whole repo, that one line failed the gate for every change regardless of what it touched - which is the state that teaches a reader to wave the gate through. This listener is always TCP, so the string case cannot arise; it narrows to null and refuses the handshake anyway, that being the safe reading, since the alternative compares the request's `Origin` against a URL ending in `undefined`.

- **The abuse limiter no longer counts per-frame transport, which had made six demo pages unusable.** Moving the pointer on `/demos/multiplayer` raised `RATE_LIMITED` on nearly every frame, and the same fault sat under `/demos/arena`, `/demos/shooter`, `/demos/kanban` and `/demos/collab-editor`. The limiter counts user actions against a budget of 100 per 10 seconds, which is right for creating a note or placing a bid; a cursor move fires on every pointer event, a smooth command on every input frame, and a doc update on every keystroke, so ordinary use spent the whole budget in about a second while an actual abuser was unaffected. The exemption had been a hardcoded list of the board's three RPC paths, written before the framework generated any of these families, so every demo added afterwards was exposed to the demo's own limiter - the board escapes only because its cursors bypass the RPC pipeline entirely and never reach the check. Exemption is now by family (`__cursor/`, `__smooth/`, `__doc/`, anchored to whole path segments) in `src/lib/server/rpc-limits.js`, so a demo added later is correct without anyone remembering the list. Still counted, deliberately: `__reaction/emit`, which is a click and a spam vector rather than transport; stream subscribes such as `__cursors` and `__presence`, which run once per room; and every action a demo declares itself.

- **The cluster tier's flash-sales, from-seq and pressure specs assert what their pages actually render.** Three of that tier's deterministic failures were recorded as cross-replica coordination bugs and are not: none of the three reaches a cross-replica assertion at all. `from-seq` required every row's message to contain `#<seq>`, but the message renders as the bare phrase with that suffix stripped, because the seq owns its own column - so the check failed on the first replica's own rows, before the two were ever compared. `pressure` opened by clearing the shed log, and the clear control is honestly disabled when the log is already empty, which on a freshly flushed keyspace it always is - the click waited out the full timeout on a button that could never become actionable. `flash-sales` gated on the stress tally appearing, but the page zeroes that tally *before* firing the burst, deliberately, so the FIFO drain is visible while it runs - so its appearance times the click rather than the outcome, and the badges were read mid-burst. The shared stock was never oversold; it was measured early. Each spec now carries the expectation the same demo's single-replica spec already encodes: the rendered-message contract, the clear-only-when-non-empty guard, and completion as the button leaving `Running...`. The cross-replica comparison in `from-seq` also gained the durable row's stored timestamp, exposed as a raw `data-ts` attribute beside the formatted clock, because once the seq is stripped from the message the surviving comparison is a pure function of the seq each page is already displaying, and would agree even between two replicas reading entirely different stores. The rendered clock cannot stand in for it: `toLocaleTimeString` discards milliseconds, so two genuinely different rows written inside the same second render as the same string, and an equality on that text stays green while the replicas disagree.

- **The arena legend's visitor colour is checked against a real visitor, and the notifications page stops crediting the wrong cluster mechanism.** The visitor row of the kind legend was only verified when another visitor happened to be drawn, which on a single-page run is never: spawns are scattered across the whole world and a client only receives what is inside its 420-unit interest radius, so the row that matters most - the teal dot that passes unexplained - went unexercised while the check reported green. A second visitor is now brought into range deliberately, by panning the spectate camera onto its position, and the equality is unconditional: no visitor drawn is a failure, not a skip. Separately, the activity note claimed that a schedule and its fire landing on different workers was the connection registry at work. It is not - that is the cluster-shared queue and the leader-gated cron distributing a job, while the registry's job is reaching a recipient's socket wherever it is connected, which is what the delivery badge reports. Teaching a mechanism through unrelated evidence leaves a confident wrong model, so the corrected attribution is pinned.

- **The board and schema-evolution specs wait for a live client before they drive it.** Thirty-eight navigations across six specs went from opening a page to clicking and typing on it with nothing in between that a server-rendered page could not satisfy. Five of them were not ungated by oversight: they call `waitForBoardReady` on the line after navigating, which reads as a readiness check and cannot fail - both of its waits end in `.catch(() => {})`, the element it waits on is server-rendered, and what remains is an unconditional 500ms sleep. A page whose client bundle never ran passes through it in half a second and the test proceeds to drive a page that will never answer, which surfaces thirty seconds later as a timeout naming a CSS selector. Each navigation now waits on the connection itself, which also routes these specs through the connection-probe diagnostics, so the next occurrence reports a timeline and names the failing asset instead of a locator. A reload is a full navigation and leaves the client just as dead, so the two `page.reload()` paths are gated as well: `note-operations`, which had been leaning on `waitForBoardReady` alone, and `schema-evolution`'s `reloadMigrated`, which three tests call immediately before clicking. Blocking the bundle on the reload only - with the HTTP cache disabled, since the production build's immutable assets would otherwise be answered from disk and never reach the abort - turns `v2-card expected 3, received 0` into a readiness failure attributed to the reload with the dead chunk named. Confirmed by construction on the test that had already failed a full tier this way: with the client bundle blocked, the ungated path reproduces that failure exactly, down to the same thirty-second timeout on the same canvas selector, while the gated path stops in fifteen seconds with the dead script named.

- **`/demos/notifications` marks the pushes that actually crossed the cluster.** The activity log named the worker that handled each step, which tells you a schedule and its fire landed on different workers but says nothing about the one thing the connection registry exists to do: carry a push to a recipient connected somewhere else. A cross-instance delivery looked exactly like a same-instance one, on the demo whose success line is "observe the cluster registry wiring". Each entry now carries the path the push took - `relayed via cluster` with the owning instance when the recipient was elsewhere, `same instance` when no hop was needed. `live.push` chooses that path internally and reports only the reply, so the page asks the registry the same question the push will ask. The comparison is against the registry's own instance id, deliberately: the registry mints an id unrelated to the leader's and writes that one into every connection row, so measuring a lookup against `leader.instanceId` would compare two unrelated random strings and label every recipient remote - including on a single-instance dev box, where no hop is possible at all. The two e2e tiers assert opposite values on the same badge, `same instance` where both tabs share one server and `relayed via cluster` on the two-replica tier, so no fixed label can satisfy both.

- **The arena minimap draws the camera's interest circle, completing the world overview.** The map already showed the world rect, the received dots and the current viewport; the fourth thing the overview needs was missing, and it is the one carrying the demo's actual claim. The circle is the same 420-unit boundary the scene draws around the camera, now at world scale, so the gap between it and the 2400x1600 frame is visibly everything the cull throws away - which is the contrast the map exists to make. It follows the CAMERA rather than the entity, so in spectate it separates from your own dot exactly as the culling does. The pin derives the map's scale and the advertised radius from the page itself rather than restating 2400x1600, 180x120 and 420 in the test, since a test carrying its own copy of those numbers agrees with a resized map that has quietly become wrong; it then asserts the circle sits on the camera while the dot stays on the entity, which a circle pinned to the entity cannot satisfy.

- **The arena kind legend and the dots it explains now read from one table, and the colours are measured.** The legend gained three labelled chips, but nothing tied a chip to the dot it describes: the chips were hand-written `bg-*` classes beside hand-written `fill-*` classes on the circles, two independent copies of the same intention. Either could be edited alone, and a legend that names the wrong colour is worse than no legend, because the visitor believes it. The kinds are now one array in the component that renders both the chips and the dot fills, so a row cannot drift on its own, and the remote dots carry `data-kind` so a test can tell an NPC from another visitor. The pin compares resolved colours rather than class names - it has to, since the chips paint with `background-color` and the dots with `fill` - and additionally requires the three chips to be three distinct colours, since a legend that paints two kinds alike distinguishes nothing while satisfying every equality. The "another visitor" row is checked whenever such a dot is on screen, which a single-page run usually has none of: another visitor appears only inside this client's 420-unit interest radius, and spawns scatter across a 2400x1600 world, so forcing one would buy a slow and flaky test for the case the shared table already settles.

- **The closing aside on `/demos/outbound-webhooks` stops clipping its declaration token.** The intro token was taught to wrap; the longest token on the page, the whole `live.webhooks.outbound(...)` declaration in the closing aside, was not. It reads as prose with spaces in it, which is how it was passed over - but the clipping risk is not a token's total length, it is its longest unbreakable run, and `live.webhooks.outbound(['demos:outbound:orders'],` contains no space at all. It overhung its container by 44px at 412 wide, losing characters off the content edge exactly as the intro token used to. Both tokens are now measured the same way, by geometry rather than by class: a `break-all` class assertion is satisfied by a `whitespace-nowrap` sitting beside it while the token clips as badly as ever, which is measured at 768px of overhang, so only the rendered line boxes settle it. The measurement compares `getClientRects()` against the parent's content box, because `<code>` is a non-replaced inline box whose `scrollWidth` and `clientWidth` are both defined as zero - the obvious form of the assertion reads 0 <= 0 and passes whatever the token does.

- **The auctions specs wait for a live page before they type into it.** Two tests walked from page-open straight to a real interaction without ever gating on the client being up, and every assertion they made on the way is satisfiable by the server-rendered page: the "alone here" badge shows whenever nobody else is present, the two empty states are what empty lists render, and the submit button's own label carries the server-rendered bidder count. A `fill()` that lands before hydration sets the DOM value while the component's state never sees it, so the submit button stays disabled and the test spends thirty seconds retrying a click on it - reported as a disabled button, which reads as a validation bug rather than as a page that was never ready. Both now gate on the page's own readiness before interacting, and the listing test additionally waits for `push-ready`, which means the socket is open AND the push handler is installed - the path a listing actually travels. Confirmed by construction rather than by reasoning: with the client bundle blocked, the ungated test reproduces the original failure exactly, down to the thirty-second timeout on `disabled ... List lot (0 bidders)`, while the gated one stops in fifteen seconds and names the cause as a page that never hydrated, with no socket ever constructed.

- **`/demos/privacy` names which round its counts belong to, so a window boundary stops reading as lost submissions.** Every number the demo publishes is scoped to a per-minute tumbling window, and nothing identified which window a reading came from. When the round tumbled while the raw aggregate was being waited on, the count restarted from zero and the wait failed with `Expected: > 1  Received: 1` - a message that fits four lost submissions and a healthy new window equally well, and that lands in a spec the failing change never touched. `roundInfo()` now returns `roundId`, the tumbling window index both aggregates use, and the page carries it as `data-round-id` on the existing round hint; there is no visible change, because a window index is not something to put in front of a visitor reading a sentence about contributors. The endpoint derives the id, the contributor count and the countdown from a single clock reading, so it cannot report one round's id beside the next round's remaining seconds. The e2e helpers read the round before the count on every poll, so a value read after the boundary can no longer settle an assertion that began before it: a crossing is raised as its own condition naming both windows, a sequence that spans one is retried in the next window, and a caller whose subject does not span it records the crossing on the run rather than failing. Widening the fresh-round margin was rejected - a minute is a hard ceiling on what fits in one window and the sequence has no bounded cost, so the boundary has to be handled rather than out-run. The crossing itself is now staged on every run instead of waited for, since one that arrives at most once a minute is otherwise exercised only by luck.

- **`/demos/shooter` keeps Enter working on focused links, and stops crediting the rewind for something it does not do.** The keyboard fire path added for visitors without a pointing device bound Space and Enter at the page level and skipped a named list of elements - inputs, textareas, buttons - which did not include anchors. A focused link therefore fired a shot and had its activation cancelled, so every link on the page and in the surrounding layout became inoperable by keyboard: one WCAG 2.1.1 gap closed by opening another. The handler now asks whether anything owning those keys has focus rather than listing what to skip, so a control added later cannot silently lose them. Separately, the latency slider's copy described a mechanism the transport does not implement. It claimed that shots land up to 400ms because the rewind covers the delay, and start missing past 400ms because the rewind clamps at `maxRewindMs`. Neither half is how it works: a shot's render-time is stamped inside `shoot()` when the send leaves, so a delay invented on the page never enters the server's rewind age and the cap cannot be reached from the app side at all. Shots do keep landing through most of the slider's travel, but because the target orbits are slow enough to keep the drift inside the hitbox, not because the wait is being compensated. The page now says that, in the caption, the intro and the module header, and the extended slider range no longer promises a boundary it cannot produce.

- **`/demos/multiplayer` picks roster and cursor label colours you can actually read.** The label rule weighted the gamma-encoded sRGB bytes with the BT.709 coefficients and compared the result against a threshold of 150. That is not how sRGB luminance works - the channels have to be linearised first - and a luminance threshold is not a contrast ratio in any case. Teal landed at 147.9 on that scale, just under the line, so it chose white: 2.49:1, against the 4.5:1 WCAG asks of normal-size text, which is what a roster chip and a 10px cursor tag are. Blue came out at 3.68:1 and orange at 2.80:1. For those draws a visitor simply could not read who was in the lounge, on one of the five surfaces the page advertises. The rule now computes real relative luminance and picks whichever of black or white measures better against the background; across the ten identity colours the worst case is 4.70:1. The previous dark candidate could not have fixed it either - restricted to it and white, three of the ten colours had no readable choice at all, which is why the fix uses true black. The rule lives in `$lib/label-contrast` with unit coverage over the whole palette, because a browser test only ever draws the single colour its visitor happened to get.

- **`/demos/pagination` keeps its Load more button under your finger, and reports the server's paging state rather than its own assumption.** Each load merged 25 rows above the button, displacing it about a viewport downward, so exhausting the 200-entry log meant click, long scroll, hunt, eight times over; the rows now grow inside their own scroll region and the control does not move at all. That button was also a borderless `btn-sm` - 32px, indistinguishable from the caption beside it, and readable as a status line rather than the unit's headline interaction - and is now a full-size primary action with the 44px coarse-pointer floor. The `hasMore` caption was a hardcoded `true` that only ever updated on a click, so until the visitor pressed anything it was rendering the client's own initializer as though it were server state; it starts as "waiting for the first page" and syncs from the store on every frame, which also makes the suite's existing `hasMore: true` assertion mean something for the first time. The cursor readout is new but deliberately partial: it states `next cursor: null` once the feed is exhausted, and names the `{ offset }` shape while pages remain, because the client store retains the cursor without exposing it and printing a reconstructed offset would be inventing a reading. Timestamps are pinned to two-digit 24-hour fields so a 12-hour locale cannot overflow the fixed time column into the seq beside it.

- **`/demos/offline` shows the queue as the visitor's own words, and stays honest when the connection is genuinely gone.** A mutation queued while offline appeared nowhere but an incremented counter, so during the demo's own hero flow - post three entries offline, reload, reconnect - the guestbook looked like it had swallowed them and the durability claim had to be taken on arithmetic. Queued posts now render as ghosted rows with a "queued" chip at the head of the list and retire when the server answers. They carry a different attribute from confirmed entries, which is load-bearing rather than cosmetic: the suite's exactly-once assertions match confirmed rows only, so a local echo can never stand in for a server round trip. The status card was driven by the in-page simulation toggle alone, so under a real network loss it kept reporting "Connected." during precisely the scenario the demo exists to show; it now reads the client's own connection state, names the outage, and distinguishes the opening handshake from an outage so no page load flashes a false alarm. That mapping lives in `$lib/offline-connection` with unit coverage, because the connecting window is a startup state no browser test against a warm server can reach. The composer's placeholder was also its only label - "(works offline)", the sentence that sells the unit, was clipped at 320 and vanished the moment anyone typed - so the input has a real accessible name and the promise is ordinary copy beneath it. The checkpoint readout glosses its own engine vocabulary, and the offline toggle is full button size with the 44px floor instead of a third smaller than the Post button beside it.

- **`/demos/chaos` pins the run you just watched, so determinism is something you can see instead of something you have to remember.** The page's own reproducibility exercise asked visitors to stop, restart, and "watch the same pattern emerge" - a check that required memorising a 60-cell random sequence, which nobody can do. A stopped run now stays pinned above the live strip with its own seed and drop-rate caption, and both rows always lay out the full 60 slots, so a re-run with the same seed repaints the pinned row cell for cell and a different seed visibly diverges from the first decision. A refused start is no longer silent: the server's reason ("Invalid seed") appears under the controls, instead of a dead button that cannot be told apart from a broken demo or a server that is down, and it clears on the next attempt. The decisions header wraps as two whole blocks rather than two colliding columns - at 320 and 360 the label and the counters interleaved into one garbled line. The seed field asks for the numeric keypad, and "random seed" wears the same bordered chip as the presets beside it instead of rendering as unpressable plain text.

- **`/demos/tenants` teaches from where the visitor stands, and spends its alarm only where it is earned.** The lede narrated from an Acme-active viewpoint while every first-run visitor arrives with no tenant at all, so the page's opening sentence described a state they were not in; it now points at the switcher, and re-reads itself once a tenant is active. The site-wide isolation caveat had the opposite problem: a saturated amber field rendered unconditionally, making it the loudest thing on screen precisely while the condition it described was false - and leaving no salience change left for the moment every demo page really is isolated to one tenant. It is neutral while that is hypothetical, amber only while it is true, and names the tenant when it is. The note composer's placeholder was doing a label's job: clipped mid-clause on every sub-768 rung and gone entirely the moment anyone typed, leaving assistive tech nothing but a placeholder-derived name. The scope clause is now a real accessible name and the placeholder is short enough to survive 320 intact. The pad title reads "Acme scratchpad" in the same vocabulary the switcher uses rather than the raw id, while the server-trusted readout keeps showing the id it actually read back, because that is precisely what it is for. The empty pad came off the metadata floor too - it is the entire first screen in a scope nobody has written to yet.

- **`/demos/privacy` is reachable alone, honest about its own numbers, and leads with the action.** The demo's headline behaviour needed three distinct contributors, and its only remedy was "open two more browsers" - impossible on a phone, where incognito windows cannot share a screen. An "Add 2 simulated contributors" control now brings two openly-labelled stand-ins into the round; they publish ordinary events under their own ids, so k is crossed by the same gate the page is teaching rather than around it. The phone-first fold is fixed too: a one-line lede leads and the nine-line mechanism paragraph collapses behind a disclosure, so the mood picker is the first thing on screen instead of a wall of spec text. Three accuracy fixes: the protected caption stated its point ("noise is added to the average and to the submission count alike - which is why the count reads 2.7"), the round hint now credits the demo-only `roundInfo()` endpoint for the distinct-contributor number instead of the raw aggregate, which only ever exposed an event count, and the reset countdown ticks every second between its 5-second polls instead of freezing and jumping five at a time beside a privacy guarantee. Mood labels became visible text and accessible names rather than hover-only titles.

- **`/demos/ops` stops inventing readings it never took.** Absent fields rendered through `?? 0` as measured zeros - "RSS MB 0" is a number no live process can produce, and every counter read 0 before the first poll resolved rather than saying it had nothing yet. Absent now renders as `-` while a genuine measured zero still renders `0`, so the dashboard's numbers mean what they say. The background-machinery rows became a real two-column grid with non-wrapping values: as a justified flex row they wrapped independently on narrow cards and a label's tail ended up beside the previous row's value. Units moved into the stat labels ("max buffered KB", "PSI cpu-some %"), which both fixes the `0KB` unit jam and keeps the four posture stats on one baseline where the column is narrow. The healthy pressure badge reads "no pressure" instead of "NONE", which sat beside "protection: normal" and bound to the wrong noun. The admin-plane curl line gains a Copy button and a scroll hint - it was clipped mid-token on phones with nothing signalling the rest existed.

- **`/demos/outbound-webhooks` narrates its retry window and puts errors where their actions live.** The demo's most instructive seconds - deliver, 500, retry, retry, dead-letter - used to happen invisibly between 3-second polls; placing a failing order now shows a live strip naming the order and the running backoff ladder, polls fast until the dead letter lands, and retires the strip when it does. Timestamps carry milliseconds, because the whole 300/600/1200ms retry ladder fits inside two wall-clock seconds and second-resolution stamps printed every attempt as the same moment. Errors split per card - a replay failure reports beside the Replay button instead of two cards above it, and a background poll hiccup can no longer overwrite either. The replay buttons rise from btn-xs to real button size with the 44px coarse floor, the DLQ card names the APIs it exercises (getDeadLetter / replayDeadLetter), and the intro's long code token wraps instead of clipping at narrow content edges.

- **`/demos/arena` earns its "world" - overview, texture, legend, and pan controls that behave like one.** A minimap in the corner shows the world frame, the camera window, and the entities the client is actually receiving - deliberately only those, because drawing the culled rest would forge the very thing the demo exists to show; the dark remainder IS the culling. A 300-unit grid gives empty stretches motion parallax and makes 2400x1600 legible as distance. The three fill colors finally name themselves (you / NPC / another visitor), and the stale freshness swatch carries alpha on its fill instead of the whole chip, so its border survives the dark theme where a quarter-opacity dot vanished. Spectate's pan controls become a real d-pad (up above down, button-sized, 44px floor on coarse pointers) instead of four undersized buttons in a row with up and down side by side - and the arrow/WASD keys that just moved the world no longer go silently dead in spectate: they pan the camera, with center reports throttled and a trailing report so the cull settles where the camera stops.

- **`/demos/notifications` shows the cluster wiring it only used to mention.** Every activity entry now names the worker that handled that step (the same instance identity the cluster-cron page teaches, stamped server-side in one place), so on a multi-replica deployment a schedule and its fire visibly land on different workers - the hop between their ids is the cluster connection registry at work, stated in a note beside the log. During a live push the sender no longer stares at a silent "Sending..." for up to eight seconds: a line names who it is waiting for and counts the reply window down. The message field gains a real label like its two siblings, the schedule slider states its 30-second domain before anyone drags to find it, the scheduled row's time-critical Cancel gets real button chrome at ack-button size instead of ghost btn-xs, and the empty states rise to readable contrast.

- **`/demos/multiplayer` tells its surfaces apart and reads on every user color.** The live headline and the editor rendered the same sentence twice with nothing naming which was which; both carry labels now ("Live value" / "Your editor - focus takes the advisory lock"), and the input's placeholder is gone because the mirror keeps the field non-empty so it could never display - the lock-state line is the visible truth the tests now assert instead of a placeholder attribute nobody ever saw. Roster badges and cursor name tags pick their label color from the background's luminance instead of fixed white-on-anything, which was unreadable on pale identity colors. The reaction bar gains outline chrome, a caption naming what a tap does, and the 44px floor on coarse pointers; the canvas dresses itself as interactive (border, crosshair, readable caption) and invites fingers as well as pointers, here and in the intro. A typing echo fires next to the input - the roster's indicator sits a full viewport away on phones.

- **`/demos/counter-resume` runs its own experiment on any browser and any pointer.** The demo's central event - a dropped connection - was reachable only through "DevTools Network -> Offline", a Chromium-desktop recipe; a "Drop the connection (10s)" button now severs the socket in-page (via a shared `$lib/simulate-offline` module extracted from the offline demo, which migrates to it unchanged), counts the outage down, and restores itself, so the freeze-then-one-frame-catch-up is a single tap everywhere. While the connection is down the page says so: the status word became a colored badge, and an "offline for Ns" line counts the outage instead of leaving a dimmed frozen number as the only signal. The ledger stopped rebuilding its entire DOM once per second - rows were keyed by list position plus value, so every prepend changed every key; they are entry-keyed now, pinned by a test that holds a row's DOM node across ticks. Ledger timestamps carry milliseconds, so two ticks landing in the same wall-clock second read as delivery jitter rather than as the sequence anomaly the gap badge exists to flag. Reset states that it resets the one counter every visitor shares, and both controls clear the 44px floor on coarse pointers.

- **`/demos/phases` leaves evidence instead of asking for faith.** The attach machine now writes a timestamped transition log under the badge - and because the page subscribes before its own auto-attach, `initialized -> attaching -> attached` is on the record for every page load, making the phases that resolve faster than a badge can flicker observable for the first time (the old copy told visitors to "watch the badge cycle after a reconnect", an instruction the page offered no way to execute; the copy now invites the Detach/Attach drill the log actually records). The fail-midway proof no longer rests on a number not moving: the failure path states outright that the first publish was buffered and retracted, with the unchanged count. Detach wears button chrome while enabled instead of ghost text next to a chrome-heavy disabled Attach, an attach failure explains itself with a retry hint instead of a raw code, the redundant "0 entries visible." line yields to the empty state, and "resume-grace window" gets its gloss plus a link to the counter-resume demo that teaches it.

- **`/demos/effect` shows the causality it claims, on every viewport and to every reader.** Audit and notification rows now wear the order that caused them: the short order id and the buyer's dot travel into both downstream feeds (the wire always carried `orderId`; the page never showed it), so correlating the three columns no longer means reading timestamps, and the dot names its buyer via `title`/`aria-label` instead of being color-only. On short viewports the feeds sit below the fold and a publish looked like nothing happening; a successful publish now leaves a receipt naming what was placed, with a jump that scrolls the feeds into view. The three action buttons wrap as one group below the inputs instead of scattering between them, the qty field is `required` so an emptied field can no longer silently place a 1x order (the server clamps a missing qty to 1, and the old page leaned on it), the column subtitles and empty states rise to readable contrast (the closing aside was already lifted layout-wide), and the title names the API once: `live.effect`: one publish, three streams.

- **`/demos/collab-editor` shows the drift instead of asserting it, hears touch selections, and stops throwing the caret.** A published offset range now carries the text it covered at publish time (the offset wire passes extra fields verbatim; the CRDT anchor wire cannot carry app fields and does not need to - staying glued is its proof), so once an edit lands in front of the range, every row rendering the frozen numbers says `selected "X"` / `now covers "Y"` in warning color, in the selecting tab and every peer alike. Selections publish from one document-level `selectionchange` listener instead of the per-textarea `select`/`keyup`/`mouseup` trio, which never fired for touch selection handles or a collapsing tap, so a phone visitor's published range went permanently stale in every other tab. A remote edit used to throw a focused caret to the end of the textarea; the textarea value is now written by an effect that owns it outright (no template binding), so it still sees the old selection when a change arrives, maps it through the same prefix/suffix diff the edits use, and writes value and caret together - while the published offsets deliberately stay frozen, because republishing the mapped range would quietly un-drift the offset panel exactly when its owner is watching it. The two-tab script is a numbered three-step list between the header and the controls instead of a sentence buried mid-intro; the intro names the panels (Offset / CRDT, not left/right) and points at the selection rows, since no in-textarea highlight exists; the empty state leads with the action at readable size and contrast; and Clear meets the 44px floor on coarse pointers.

- **`/demos/shooter` works on every input, acknowledges every shot, and lets the cap be felt.** Touch visitors get the same four-way movement pad the arena demo grew (now a shared `MovePad` component both games render; arena's markup and testids are unchanged), and keyboard visitors get a fire path - Space or Enter shoots toward the nearest rendered target, so the demo's single success action no longer requires a pointing device. Every fire draws an immediate info-colored receipt ring at the aimed point, distinct from the warning-colored muzzle flash and impact spark, so a click during high slider lag reads as "received, queued" instead of dead. The latency slider now runs to 600ms with the 400ms cap marked on the track: past the mark the rewind clamps and aimed shots start missing, which is the only way a cap teaches itself (the old prose promised hits across the whole range, which the extension makes deliberately false, and was rewritten). The score card shows your own hp - the victim half of `onHit` was previously invisible - a small "you" label rides your dot so the first shot's aim is not a guess, and the intro says "click inside the arena" now that "range" only ever means the slider.

- **`/demos/cluster-cron` says who the leader is instead of making the visitor prove it.** The "Current cron leader" cell carries a "this instance" badge when the newest tick belongs to this worker (before, verifying the page's central claim meant prefix-matching hex ids across two cells), and the leader-status badge derives live from the tick stream instead of freezing its mount-time value, so a takeover flips it for the rest of the session. The lease key renders quoted so its literal value stops reading as a third "leader" status field. The per-row "(this instance)" annotation - one bit repeated thirty times, and the exact label that crushed to "(th ins" at 320px - is gone; the badge hue carries ownership and a one-line legend states the mapping once. A new "The wiring" panel shows the three-line `createLeader` + `configureCron` + `live.cron` setup the page teaches, and corrects the prose's `live.configureCron` to the `configureCron` export that actually exists. Tick times pin two-digit 24h fields so 12-hour locales stay inside the fixed time column (covered by an en-US-forced spec). The takeover recipe drops its warning tint - it is a recipe, not a hazard - so the live tick log stays the strongest center on the page, and it ends with a normal-size pointer to the durable jobs demo, previously reachable only through a faint aside link.

- **The e2e connection probe re-arms per wait instead of replaying the first one.** Each wait ended by stopping the probe's sampler but left the probe installed, and the installer bailed out early whenever it found one. A second wait on the same page - every reconnect test, every repeated wait after a mid-test operation - therefore got the FIRST wait's timeline, with every timestamp measured from a `t0` that could be minutes in the past and no samples taken since that first cleanup, plus sockets that had already been accounted for. It now resets the same probe object rather than building a new one, which keeps the single `WebSocket` wrapper in place: wrapping again per wait would nest the constructors, and each nested layer would record into whichever probe it had closed over instead of the live one. Covered by a regression test that fails on the exact staleness - two waits reporting an identical `t0`.

- **The checkout reset test stopped reading its baseline before the value arrived.** `/demos/checkout` delivers one fact two ways: the RPC history row is local state appended when the response returns, while the big counter only moves when the live stream's publish lands. The reset test clicked Place Order, waited for the history row, and read the counter as its baseline - sampling a number the publish had not reached yet. The publish then arrived during the confirmation round trip, so the assertion that a *dismissed* reset changes nothing compared a settled count against a stale baseline and failed with an off-by-one, intermittently, depending only on whether a sub-100ms round trip won. The baseline is now read before the click and the increment is waited for, which is what the two tests above it already do.

- **The privacy demo's tests no longer straddle the round they are measuring.** Everything on `/demos/privacy` is scoped to a tumbling wall-clock-minute round, and at the boundary the raw window restarts at zero - which the page renders as no panel at all, so the count reads as 0 rather than as "a new window". `waitForFreshRound` guarded only against inheriting a round that had already reached k; it read the seconds remaining and never used them, so it would hand back a round with three seconds left and the following five-submission sequence would be counted into a window that then tumbled away. It now requires a round that has *just* tumbled, not merely one with time left: a submission waits for an aggregate publish, so five of them cost around 25 seconds against a 60-second round, and 20 seconds of headroom was measured still straddling. Both conditions become true together at every boundary, so the wait costs at most one round. Deliberately fixed at the helper rather than by making the submission assertion tolerate a reset, which would have retired a real check.

- **The from-seq replay probe no longer fails on cron drift before reaching its subject.** `test/e2e/_from-seq-replay-probe.spec.js` waited for exactly twenty rehydrate events before exercising the pause and resume it exists to capture. That count is a function of how long the 1Hz cron has been ticking when the page opens, and nothing controls it - the harness gates each tier on the cron having STARTED, never on it having produced a given number. Three runs settled at twenty, sixteen and four, so the probe reported a red diagnostics tier for a few seconds of drift while its actual subject went unexercised. It now waits for the count to stop moving and reports where it stopped, with a floor of merely greater than zero: a smaller fixed number would be the same mistake one size down.

- **Board presence stopped sticking at "1 here" after the last member left.** The board list renders a presence badge per board, and the Redis presence extension treats any subscribe to a non-`__presence:` topic as a member JOIN - so every card observing a board's roster was counted as a member of it. `PresenceBar` already owns the real lifecycle (join on mount, leave on cleanup), so the app's subscribe hooks now skip the plugin join for the bare `board:<id>` topic and let the observer path authorize through `platform.checkSubscribe` instead. Sub-topics (`:notes`, `:settings`, `:activity`) and socket close are unaffected.

- **Lobby rosters and owner succession release on an explicit unsubscribe.** This app owns the adapter's `unsubscribe` hook, which REPLACES the framework's rather than composing with it, so realtime's managed-topic drain never ran on a wire unsubscribe and only fired at socket close: a visitor who left a table stayed on its roster, and an owner who left never handed the room on. The hook now chains `unsubscribe` from `svelte-realtime/server` before the extension's presence cleanup. Note one upstream consequence, filed against the framework: realtime records drained topics per socket and does not clear that record when the same socket re-subscribes, so a leave-rejoin-close cycle on one connection skips the close-time drain. The demo's own regression coverage exercises that path.

- **`/demos/arena` reported "of 0 entities" on non-authority replicas.** The HUD denominator came from a module-level cell written by the smooth authority's tick, but `SO_REUSEPORT` gives no guarantee that a page's RPC lands on the process running the authority, so three replicas out of four answered zero. The tick mirrors catalog-size changes into Redis and the RPC reads the shared value, cached for a second per process so a polled HUD line does not become one Redis round trip per open tab, and falling back to the local cell rather than surfacing an infrastructure error if the read fails.

- **Re-uploading an identical file no longer double-counts its bytes or strands an order entry.** Ids are content-addressed, so the second upload of the same file appended a duplicate list entry and added its size to the shared byte counter again, permanently inflating the total against the 50 MB cap. Append and remove are each one atomic Redis script now, as is each eviction step - previously a replica that read a record just before another replica removed it still subtracted its bytes, and once the counter went negative the byte cap stopped being enforced and the page rendered a negative total. The stale counters repair themselves on the next append or removal. The page's notification list is also keyed per notification rather than per file, so a repeat upload cannot collide with itself and blank the banner.

- **`/demos/privacy` no longer claims to have published when it has not.** The protected aggregate's initial serve is an un-noised zero seed, and the wire cannot distinguish that from a real held value, so a pristine server rendered `0.00 (last published value)` for a publish that never happened. The round RPC reports whether this process's aggregate has actually crossed k, and the card holds until it says so. The flag is deliberately per-process, matching the lifetime of the in-memory aggregate state it describes: a cluster-wide durable flag would outlive that state and tell a restarted replica's visitors "published" while showing them the seed.

- **`/demos/chaos` no longer mixes two runs in one strip.** A new run restarts tick ids at 1, and with a keyed CRUD merge those rows overwrote the previous run's in place while its later rows survived - so the tick counter, delivered count, and empirical drop rate were read off a stale row until the new run reached 60 ticks. Starting a run now refreshes the stream first.

- **`delta.fromSeq` results actually merge.** A `delta.fromSeq` bridge must return `{ event, data, seq }` envelopes: a `replay: true` array response is fed straight into the client's merge, which dispatches on `envelope.event`. The from-seq demo returned bare rows, which carry no `event` and silently applied nothing - the tier had never rendered a row.

- **Readability and layout fixes across the gallery.** The navbar's end cluster no longer paints over the wordmark between 640 and 1023 (the online count and colour picker wait for `lg`), and the wordmark truncates rather than being covered. Demo explainer asides were 12px at 50% opacity - about 3.4:1 on light, under the AA bar - and now render at 14px / 70%. The board canvas empty-state hint derives its ink from the board's own background rather than the theme, at an alpha that clears 4.5:1 on all six palette values. Hostile board titles truncate instead of widening the page (and the create field caps at the length the server already enforced). Plus per-page fixes at phone and tablet widths: the chaos decision strip fits 60 cells at 320px, pagination rows wrap and confirm an append through a live region, the pressure shed log scrolls instead of clipping its reason column, `flash-sales` stock badges stop being clipped, `lobbies` inputs carry real labels, and `tenants` shows the effective wire topic.

- **`/demos/offline` demonstrates itself without DevTools.** A first-time viewer on a live connection saw the queue stay 0 and nothing of the offline -> reconnect -> dedup story, because the interesting behavior only happened if you manually toggled DevTools offline. Added an in-page **Go offline / Reconnect** toggle (plus a live offline/online banner and guiding copy): it drops the live socket - the same transition that arms the realtime queue - and blocks reconnection with a temporary `window.WebSocket` stub until Reconnect, so posts made in between queue in IndexedDB and replay exactly once on reconnect, visibly, in one click. Uses only supported client behavior (the adapter client already drops on the browser `offline` event and reconnects on `online`); `maxReconnectAttempts` defaults to Infinity, so the blocked reconnect loop can never wedge into a terminal state, and the block is torn down if the viewer navigates away mid-simulation. A new `demos-offline.spec.js` case drives the toggle end to end (queue two posts, reconnect, each lands exactly once). This is a demo affordance; a first-class client-side simulate-offline primitive is the proper long-term home.

- **`/demos/ops` now names the replica it read.** `introspect()` is per-process by design (registry sizes, connection count, cron-running, RSS are all local to one worker), so on the SO_REUSEPORT cluster each reconnect can land on a different replica and the counts swing between refreshes - correct behavior that read as "is this broken?" on a showcase. The snapshot RPC now stamps `replica: leader.instanceId` (the same worker id `/demos/cluster-cron` shows), and the page renders "reading replica X" beside the refresh time plus a one-line note that the numbers are per-worker. `leader.instanceId` is the extensions leader-lease identity; it is null only before init / during build analysis, in which case the readout is hidden.

- **`/demos/multiplayer` headline now commits the typed value.** Clicking the Set-headline button blurred the input, which flipped `editing` false and let the "mirror the live headline into the draft" effect overwrite the just-typed text with the current (default) headline before the submit handler read it - so the RPC published the default. The submit button now suppresses the focus steal (`onmousedown` preventDefault), so the input keeps focus through the submit and the draft survives.

- **Demo purge crons now actually run (they never had).** The general (every 30 min) and upload (every 5 min) purge crons lived in `src/lib/server/demo-purge.js`, outside `src/live/` - the only tree the realtime codegen scans to emit the cron registry - so `live.cron()` attached its schedule but nothing ever registered or ticked it. Weeks-old chat and other demo content accumulated in prod as a result. Moved both crons into `src/live/_purge.js` (registered form: `export const X = live.cron(...)` with the env kill-switch enforced in the handler instead of a ternary the codegen regex would not match); the boot-time eager demo import moves with it. Defense in depth: chat room LISTs now carry a 2h backstop TTL refreshed on each push (`src/live/demos/chat.js`), so an abandoned room self-expires even if a cron regresses. Regression guard: `npm run check` now fails if any `live.cron(` is defined outside `src/live/` (`scripts/check-cron-registration.mjs`). Verified: the built registry emits `__registerCron("_purge/purgeAllDemos")` / `purgeUploadCron`, and a 1-minute-interval production run logs `[demo-purge] tick at ... (failed=0)`.

- **`/demos/multiplayer` reactions no longer snap back to spawn or revive on each tap.** The reaction bar rendered the server's bounded reaction ring keyed by object identity (`{#each room.reactions as r (r)}`). Every push re-keyed the whole list, so Svelte tore down and rebuilt every emote node and restarted the float animation: emotes already floating snapped back to their spawn point and already-faded ones reappeared. The page now reconciles the ring into a locally-keyed list - a stable id per genuinely-new frame, dropped on a timer once its animation has run - so a tap appends exactly one emote and leaves the others floating undisturbed. A new `demos-multiplayer.spec.js` case guards it by asserting the first emote's float animation keeps advancing across a second, different tap instead of rewinding toward zero.

- **`/demos/kanban` board survives a reload.** The document declared no `persist` hooks on the assumption that the in-memory server replica lives for the process lifetime; in fact the framework releases a document's replica once its last subscriber disconnects, so a lone visitor's reload reset the whole board. Added a Redis snapshot `persist` (24h TTL, same shape as `/demos/collab-editor`), which also bounds the previously-unbounded public board.

- **`/demos/flags` reads the flag stores directly again.** With the realtime SSR flag stub now published, the page dropped its `onMount` + manual-subscribe workaround and reads `$banner` / `$darkLaunch` straight off the stores.

- **Page-local `configure()` calls no longer drop the app-wide client options.** `configure()` replaces the realtime client config wholesale, so the upload page's `configure({ upload: { frameSize } })` silently un-set the resume-grace window and the protocol version the root layout had established (the offline page worked around it by re-asserting both inline). New `$lib/configure-app.js` exports `configureApp(options)`, which merges the app defaults under the page's options; the layout, the upload page, and the offline page all go through it now.

- **`docker-compose.yml` sets `NODE_ENV=production` for the `app` service.** Pre-fix, the container had `NODE_ENV` unset; Node read it as `undefined`. svelte-realtime gates its dev-only publish-rate-warning sampler on `process.env.NODE_ENV !== 'production'` (treats anything other than `'production'` as dev). With the gate open, every call to `_getCtxHelpers(platform)` armed a `setInterval` for that platform and registered the platform in a strong-reference `Set` (`_publishRateActivePlatforms`). Combined with the cron tick's per-fire `bus.wrap(_cronPlatform)` minting a fresh wrapped platform every second on the leader, the Set accumulated one entry per fire at ~7 Hz across the news / topk / cluster-cron / notifications / pressure crons. Each retained entry kept the wrapped platform's full helpers + getters + an active timer alive. Heap grew by ~100 MB/hour on the leader worker until OOM. Setting `NODE_ENV=production` constant-folds the dev branch dead and the leak path stops at boot. Existing `bus.wrap`, `_getCtxHelpers`, and `_publishRateActivePlatforms` shapes upstream are unchanged; this fix is purely an environment correction. (Defense-in-depth follow-up belongs upstream in svelte-realtime: `_publishRateActivePlatforms` should be a `WeakSet`, not a `Set`, so a forgotten `NODE_ENV` cannot leak in dev either.)

- **`src/lib/server/upload.js` chunks Map switched to a Set of hashes only; bytes are no longer retained.** Pre-fix, every uploaded chunk's raw `Uint8Array` (up to 1 MB) stayed in a module-level `Map<hash, Uint8Array>` until `purgeMemory()` cleared it. Purge runs on a leader-gated cron, so non-leader workers in a clustered deploy never cleared their local Map and the Map grew without bound at upload rate. Bytes were also never read back (the demo's wire surface returns dedup metadata only; `getChunk` was exported but had no callers). The new shape keeps only the hash strings (Set membership for dedup checks), which is bounded at `MAX_FILES * avg-chunks-per-file` entries (~KB total). Removed the unused `getChunk` export; `storeChunk(hash)` no longer accepts the bytes argument.

- **`/demos/chaos` `states` Map now cleared on WS close.** Pre-fix, the demo's per-user state Map (`states: Map<userId, ...>`) was documented as "demo accepts the leak": every user who ran `startChaos` and disconnected without clicking Stop left an orphan entry that the 100ms ticker iterated forever, publishing into a topic with no subscribers. Anonymous demo users get a fresh `ctx.user.id` per session, so orphans accumulated one per session. Added an `onClose(ws)` export wired from `src/hooks.ws.js` close handler that calls `states.delete(ws.getUserData().id)`. Same fix shape that other in-process demo state should use; chaos was the only such demo.

- **`docker-compose.yml` now plumbs the 0.6 env vars into the `app` container.** The compose `environment:` block never forwarded `ADMIN_TOKEN`, `DEMO_OUTBOUND_WEBHOOK_SECRET`, `ROOMS_CODE_SECRET`, or `SSL_WATCH`, so values set in the operator's `.env` never reached the app process (Compose only forwards keys it interpolates). The most visible effect: the fail-closed `/__realtime/*` admin plane read `env.ADMIN_TOKEN` as unset and denied every request, including a correct bearer. All four are now forwarded as `${VAR:-}`, so `.env` values flow through and unset stays a safe default (admin plane fail-closed, optional secrets on their dev fallbacks).

- **TLS hot-reload (`SSL_WATCH`) defaults off for the host-networking deployment.** `svelte-adapter-uws` next.75 defaults `SSL_WATCH=1` - a restartless in-process cert reload that swaps the served cert as a uWS SNI server name. Under this stack's `network_mode: host` + SO_REUSEPORT layout that swap leaves the listener refusing external TLS handshakes (clients see the connection closed mid-handshake), even though the process logs a successful `:443` bind. Renewal here is already restart-based (certbot + `entrypoint.sh` graceful-restart on a fingerprint change), so the adapter watcher is redundant; `docker-compose.yml` and `.env.example` default `SSL_WATCH=0`. (Upstream follow-up: the adapter's SNI-swap reload should coexist with host-networking clustering so `SSL_WATCH=1` is safe there.)

### Added

- **SIGUSR2 heap-snapshot trigger in `src/hooks.ws.js`.** `kill -SIGUSR2 <node-pid>` on the host writes a `heap-<timestamp>.heapsnapshot` file to the worker's CWD, loadable in Chrome DevTools (Memory tab -> Load profile). Synchronous stop-the-world pause for the snapshot (~50-500ms depending on heap size) and the signal isn't reachable from outside the host. Useful for heap-retention diagnostics on a live worker.

### Security

- **App connects to Postgres as the non-superuser `stickynotes_app` role.** Pre-fix, the demo's `DATABASE_URL` connected as `postgres` - the superuser bootstrap role created by the official postgres image. An RCE landing inside the node container had full DDL / role-management / cross-database access from the connection it already held. Fix: a new `init-app-role.sh` runs once on first postgres init (via `/docker-entrypoint-initdb.d/00-init-app-role.sh`) and creates the `stickynotes_app` role with the password from `STICKYNOTES_APP_PASSWORD`. `schema.sql` runs a `SET ROLE stickynotes_app` block so the `board` / `note` tables, the `note_board_id_idx` index, and the `archive_old_notes()` function are all owned by `stickynotes_app`. Owner privilege lets the runtime `ALTER TABLE` in `hooks.server.js` continue to work without granting the app role broader rights. The role is NOT a superuser (`rolsuper=f`), cannot create other roles (`rolcreaterole=f`), cannot create databases (`rolcreatedb=f`), and has no privilege on databases other than `stickynotes`. The `postgres` superuser password (`POSTGRES_PASSWORD`) is still required for the image's first-init bootstrap and for operator ad-hoc maintenance (`docker exec -it <container> psql -U postgres`), just not for the app's runtime connection.

  **Operator action required (existing deployments).** docker-entrypoint-initdb.d only runs on fresh volume init, so existing deployments must create the role manually and transfer table ownership ONCE before redeploying:

  ```bash
  # 1. Set STICKYNOTES_APP_PASSWORD in .env (a fresh 32-char random string is fine).
  # 2. Run this from the existing deployment host:
  docker compose exec postgres psql -U postgres -d stickynotes <<'SQL'
    CREATE ROLE stickynotes_app WITH LOGIN PASSWORD 'paste-STICKYNOTES_APP_PASSWORD-here';
    GRANT USAGE, CREATE ON SCHEMA public TO stickynotes_app;
    ALTER TABLE board OWNER TO stickynotes_app;
    ALTER TABLE note OWNER TO stickynotes_app;
    ALTER FUNCTION archive_old_notes() OWNER TO stickynotes_app;
  SQL
  # 3. docker compose up -d (recreates the app container with the new DATABASE_URL).
  ```

  Fresh deployments (operators wiping the data volume or starting from scratch) get the role created automatically by the init script - no manual step needed.

- **Identity moved from a JSON cookie to a server-side session keyed by an opaque cookie-id.** Pre-fix, the identity cookie was plain JSON; the `/demos/denials` org ACL is keyed off `userData.org` which derives from this cookie, so any browser-side attacker who controls their own cookie can switch orgs at will. Considered HMAC-signing the payload (and shipped a prototype), then refactored to the canonical session-store pattern: the cookie is now a 128-bit opaque base64url session-id (~22 chars), and the actual identity (`id`, `name`, `color`, `org`) lives in Redis under `identity-session:<id>` as a hash with a 30-day sliding TTL. Forgery requires guessing a 128-bit random id (computationally infeasible). Compared to HMAC, this pattern gains: trivial per-user revocation (`DEL identity-session:<id>`), trivial field mutation (`HSET identity-session:<id> org globex` for the org-switch endpoint), and no long-lived shared secret to rotate or distribute across replicas. New module `$lib/server/identity-session` exports `lookupSession`, `createSession`, `updateSessionField`, plus `tryParseLegacyJsonCookie` for one-shot migration of pre-this-change visitors (their displayed name carries over). Wired through `+layout.server.js`, `hooks.ws.js`, `api/demos/set-org/+server.js`. The `COOKIE_SECRET` env var added in the earlier iteration is removed - the new pattern needs no secret. svelte-check passes with zero errors.

- **`/metrics` endpoint gated by `METRICS_SCRAPE_TOKEN` when set.** Pre-fix, the metrics endpoint was always reachable. Fix: when the env var is set, the handler requires a matching `X-Scrape-Token` header and 401s otherwise. When unset (default), the endpoint stays open - no behavior change for existing deployments. The endpoint's own comment already recommended gating; this implements it. `.env.example` documents the option.

- **Webhook secret fails closed in production.** `src/live/demos/news.js` previously fell back to a hardcoded `'demo-news-secret'` string when `DEMO_NEWS_WEBHOOK_SECRET` was unset, including in production - so the secret used to verify HMAC-signed webhook events was a value checked into the repo. Fix: the fallback now throws at module load when `NODE_ENV === 'production'`. Dev keeps the same fallback so a fresh checkout works without env setup.

- **Postgres bound to `127.0.0.1`.** Pre-fix, `docker-compose.yml` exposed Postgres on `5432:5432` (= `0.0.0.0:5432` on the host); operators with permissive host firewalls had a direct DB connection surface from any peer. Fix: port mapping changed to `127.0.0.1:5432:5432`. The app continues to reach Postgres via the docker bridge network (DNS name `postgres:5432`, see the network refactor below). External `psql -h <host>` from another machine is now blocked; operators run `docker compose exec postgres psql ...` or open a sidecar tunnel when DB access from the host machine is needed. Mirrors the docs repo's pre-existing 127.0.0.1 bind.

- **App container no longer uses `network_mode: host`.** Pre-fix, the app shared the host's network namespace and connected to redis/postgres at `127.0.0.1:6379` / `:5432` (the host's loopback). This works but couples the app to the host's network stack and conflicts with the localhost-bind changes (the loopback `127.0.0.1:6379` mapping on the redis service was bypassed by host networking - the app was hitting redis via the host's docker-proxy port mapping anyway). Fix: drop `network_mode: host`, publish port 443 explicitly via `"443:3443"` (container listens on 3443 as the non-root `node` user; docker-proxy handles the host-side 443 binding), and resolve redis/postgres via the docker bridge network's embedded DNS (`redis:6379`, `postgres:5432`). **Operator action required**: this is a coordinated networking change. Verify in staging that TLS handshake completes on https://your-domain before deploying to production. Cert volume mount (read-only at `/etc/letsencrypt`) is unchanged. Performance: one extra docker-proxy hop on inbound TLS, negligible for WebSocket/SSE-shaped workloads.

- **App container runs as the unprivileged `node` user.** Pre-fix, the demo's Dockerfile had no `USER` directive, so the app ran as root inside the container - giving any RCE inside node access to the full container's filesystem and capabilities. Fix: install `gosu`, copy a new `entrypoint.sh` that (a) copies the Let's Encrypt privkey (mode 0600 root) into a node-readable location and rewrites the SSL_CERT/SSL_KEY env vars to point at the copy, (b) execs `gosu node node build` to drop privileges before the app starts. Mirrors the docs repo's pattern. The image's port stays at 3443 (non-privileged) so no `cap_net_bind_service` is needed; docker-proxy handles the host:443 binding. **Operator action**: ensure your deployment workflow runs `docker compose build` to rebuild the image with the new entrypoint; the running container needs replacement.

### Changed

- **Identity cookie now has explicit `secure: !dev` and `sameSite: 'lax'`.** Previously the `cookies.set('identity', ...)` calls in `+layout.server.js` and `api/demos/set-org/+server.js` set `path` / `maxAge` / `httpOnly` but relied on SvelteKit defaults for `secure` and `sameSite`. SvelteKit's defaults are reasonable but explicit beats implicit; this future-proofs against a SvelteKit default-flip and makes the intent visible at the call site. `secure: !dev` means cookies require HTTPS in production but are allowed over HTTP in dev mode where the dev server runs at localhost.

- **`build.sourcemap: false` set explicitly in `vite.config.js`.** Vite's default depends on env/mode and could swap to `true` under a debug build path. Pinning to `false` prevents a future config drift from leaking server-source maps into the production bundle.

- **Docker base images pinned by digest.** `node:22-trixie-slim`, `postgres:17-alpine`, and `redis:7-alpine` in `Dockerfile`, `Dockerfile.postgres`, and `docker-compose.yml` are now suffixed with `@sha256:<digest>` so reproducible rebuilds cannot accidentally pull in a re-tagged base image. Bump the digests manually when the operator decides to pull in upstream security fixes; the existing `docker compose build` does not auto-resolve to newer image versions once pinned.

- **Production image installs only runtime dependencies.** Pre-fix, the production stage copied `node_modules` from the build stage, carrying every devDep (vite, svelte-check, playwright, etc.) into the runtime image. Fix: the production stage now runs `npm ci --omit=dev` directly against the same `package-lock.json`, then copies only the build artifacts (`/app/build`). Net result: a smaller image, zero devDeps shipped, and no chance of a future test/build tool getting executed at runtime. The build stage still installs full deps (it needs vite to build).

- **Redis bound to `127.0.0.1` and supports optional `REDIS_PASSWORD` AUTH.** Pre-fix, `docker-compose.yml` ran redis with `--protected-mode no` AND port mapping `6379:6379` (= `0.0.0.0:6379` on the host), making the cache directly reachable from any peer with network access to the host. This was a real exposure for deployments where the host firewall doesn't block 6379. Fix: (a) port mapping changed to `127.0.0.1:6379:6379` so only the host can reach redis, (b) `--protected-mode no` dropped (no longer needed, and keeping the default fail-closed posture is a defense layer for future misconfigurations), (c) the redis service's `command` now branches on `REDIS_PASSWORD`: when set, starts with `--requirepass`; when unset, starts unauthenticated (preserves existing-deployment behavior - no operator action required at upgrade time). The app's `REDIS_URL` mirrors with `redis://:${REDIS_PASSWORD:-}@127.0.0.1:6379`. Healthcheck also adapts. `.env.example` documents the option. Operators wanting AUTH set `REDIS_PASSWORD` in `.env` and recompose. Existing deployments without the variable keep working unchanged but now without the public 6379 surface.

- **`POST /api/demos/set-org` rejects cross-origin requests at the application layer.** SvelteKit's built-in CSRF check (in 2.59.x and earlier) fires only on form-content-types: `application/x-www-form-urlencoded`, `multipart/form-data`, `text/plain`, and SvelteKit's binary-form type. JSON POSTs are skipped by intent - the framework relies on browser CORS preflight + the `sameSite: lax` cookie attribute to defend cross-site CSRF for JSON. Both layers already protect this endpoint in practice (the identity cookie is sameSite: lax; no permissive CORS headers are set), but a future cookie-attr regression (sameSite: none) or a non-browser client bypassing CORS would reopen the gap. Defense-in-depth: the handler now reads `request.headers.get('origin')` and errors 403 if non-null and non-matching `url.origin`. Same-origin requests (the only legitimate use of this endpoint - the `/demos/denials` org-picker) continue to work unchanged.

- **`.gitignore` now excludes `*.pem`, `*.key`, `*.crt`, `*.pfx`, `*.p12`.** Pre-fix, only `.env*` was ignored - so Let's Encrypt artifacts (`fullchain.pem`, `privkey.pem`, `cert.pem`), self-signed dev certs from `init-certs.sh`, and Windows certificate bundles (`.pfx`, `.p12`) would slip into a commit on `git add .` from the wrong terminal. None are currently tracked (verified via `git ls-files | grep -iE '\.(pem|key|crt|pfx|p12)$'`), so the change is preventative rather than remediating - no historical commits need scrubbing. The same patterns landed in the docs repo's `.gitignore` under the same date.

- **`e2e/destroyer-standalone.js` no longer disables TLS verification process-wide.** Pre-fix, the script set `process.env.NODE_TLS_REJECT_UNAUTHORIZED = '0'` at module load, which disables certificate verification for EVERY subsequent TLS handshake in the process - not just the destroyer's own fetch calls. The WebSocket open at line ~50 already used a scoped `rejectUnauthorized: false` per connection; the global mutation was the remaining bypass. A future maintainer adding a fetch to a third-party endpoint (a metrics push, a webhook callback, anything) would unknowingly skip cert verification on that endpoint because the global toggle was still set. Fix: import `Agent` from `undici` (Node's bundled HTTP client; added as devDependency so the Agent class is reachable via public ESM import), construct a single `insecureDispatcher = new Agent({ connect: { rejectUnauthorized: false } })` for this script's two `fetch` calls (`getBoardId` and `checkServer`), and remove the global env mutation. Each fetch now passes `{ dispatcher: insecureDispatcher }`. The destroyer's behavior against self-signed certs is unchanged; what changed is that the bypass is now bounded to the two endpoints the destroyer actually hits.

### Changed

- **All six top-level shell scripts (`deploy.sh`, `init-certs.sh`, `renew-certs.sh`, `reset-all.sh`, `reset-db.sh`, `reset-redis.sh`) switched from `set -e` to `set -euo pipefail`.** Plain `set -e` catches only direct command failures in straight-line code; it misses (a) failures in the left side of a pipeline (the `false | tee` shape - the script proceeds even though the upstream errored) and (b) unset-variable references (a typo like `$DOAMIN` silently expands to empty string and feeds the empty string into `docker compose exec`). `set -u` (nounset) makes the typo error out; `pipefail` propagates pipeline failures. Audited each script for compatibility before flipping: the only scripts that read environment vars are the reset trio (`${CONFIRM:-}` is `set -u`-safe) and `init-certs.sh` (`${1:?...}` / `${2:?...}` are also `set -u`-safe by design). Pure `docker compose` driver scripts with no variable reads inherit the safer semantics for free.

### Security

- **`reset-all.sh`, `reset-db.sh`, `reset-redis.sh` now require `CONFIRM=yes` to run.** Pre-fix, anyone with `docker exec` access (or shell access to the operator's checkout) was one `./reset-all.sh` away from `TRUNCATE note, board CASCADE` plus `redis-cli FLUSHALL` against the running demo. A muscle-memory `./reset-` followed by a tab-complete in the wrong terminal pane would wipe production demo state with no second prompt. The scripts now check `[ "${CONFIRM:-}" != "yes" ]` before any destructive command and exit 1 with a usage hint; the new invocation is `CONFIRM=yes ./reset-all.sh` (or `./reset-db.sh`, `./reset-redis.sh`). The guard fires before `cd "$(dirname "$0")"` so even a script invoked from the wrong directory cannot proceed accidentally.

- **`reset-*.sh` excluded from the prod Docker image via `.dockerignore`.** Pre-fix, the scripts shipped inside the container at the workdir root (no `.dockerignore` rule excluded them) and were reachable via `docker exec demo-app bash /app/reset-all.sh`. Even with the new `CONFIRM=yes` guard at the script level, the principle is that operator tooling for tearing down dev data has no business being inside a production image - so the scripts are now stripped at build time by adding `reset-*.sh` to `.dockerignore`. Operators run them from the host checkout (where the scripts still live and the guard still applies).

## [0.4.0] - 2026-03-20

Aligns with svelte-adapter-uws 0.4.0, svelte-adapter-uws-extensions 0.4.0, and svelte-realtime 0.4.0.

### Breaking Changes

- **`hooks.ws.js` must export an `unsubscribe` hook.** Required by svelte-realtime 0.4.0. Without it, presence and cursor cleanup only happens on socket close, not on topic drop. Our implementation delegates to `presence.hooks.unsubscribe()`.
- **`close` hook signature changed.** The adapter now passes `{ platform, subscriptions }` instead of just `{ platform }`. Our hook now forwards the full context object to `presence.hooks.close()` and `cursor.hooks.close()` instead of destructuring.
- **Removed bare `export { presence, cursor }` from `hooks.ws.js`.** These module-level re-exports did nothing. All hook wiring is now explicit through named hook functions.
- **`cursor.remove(ws, platform)` replaced with `cursor.hooks.close(ws, ctx)`.** The extensions 0.4.0 cursor plugin exposes a `hooks` helper. Direct `.remove()` calls are replaced with the hooks pattern.
- **Dependency versions bumped.** `svelte-adapter-uws` from ^0.3.8 to ^0.4.0, `svelte-adapter-uws-extensions` from ^0.1.9 to ^0.4.0, `svelte-realtime` from ^0.1.7 to ^0.4.0. All three must be upgraded together.

### Added

- **`unsubscribe` hook** in `hooks.ws.js`. Fires the moment a client drops a topic (page navigation, stream teardown). Cleans up presence for that topic immediately instead of waiting for the socket to close. Users disappear from board presence bars the instant they navigate away.
- **Cursor snapshot on subscribe.** `cursor.hooks.subscribe()` is now called in the `subscribe` hook. When a user joins a board, they receive the current cursor positions of everyone already on that board instead of waiting for the next movement from each user.
- **Circuit breaker for Redis** via `createCircuitBreaker()` from `svelte-adapter-uws-extensions/breaker`. Wraps presence, cursors, pub/sub bus, and rate limiting. After 5 consecutive Redis failures, the breaker trips and operations fail fast. Probes again after 30 seconds. The app stays functional without Redis, just without cross-instance features. State changes are logged to the console.
- **`ctx.batch()` for arrangement actions.** Tidy, rearrange, shuffle, and group-by-author now publish all note updates plus the activity event in a single `ctx.batch()` call. Reduces Redis pub/sub round trips from N+1 to 1 when running multiple replicas.
- **`ctx.batch()` for cleanup cron.** Stale board deletions are published in a single batch instead of one-by-one.
- **Postgres advisory lock on cleanup cron.** With multiple replicas, only one acquires `pg_try_advisory_lock(900001)` and runs the cleanup. The others skip the tick. Prevents duplicate deletes and duplicate event broadcasts.
- **`tryAdvisoryLock()` and `advisoryUnlock()` in `db.js`.** Non-blocking advisory lock helpers. Return `true` immediately in dev mode (no Postgres) so crons run normally without a database.
- **Input validation guard for update payloads.** `validateBoardFields()` and `validateNoteFields()` now reject `null`, strings, arrays, and other non-object values with a clean `VALIDATION` error instead of throwing a raw TypeError.
- **First-load hint in dev mode.** A Vite plugin logs `[demo] First page load compiles all modules on demand -- expect 5-10 seconds. Subsequent loads are instant.` at server start so developers know to expect a slow first render.
- **Docker Compose runs 2 app replicas** again (was temporarily set to 1). Exercises the Redis pub/sub bus and advisory lock in production.

### Fixed

- **Startup race between migration and `ensureBoard`.** Previously, the `ALTER TABLE` migration and `ensureBoard('stress-me-out')` fired concurrently. If `ensureBoard` hit the database before the migration finished, it would fail with "column last_activity does not exist". Migration now completes before `ensureBoard` runs.
- **Dead migration code removed.** The `UPDATE board SET last_activity = created_at WHERE last_activity = created_at` query after the ALTER TABLE was a leftover from the initial backfill. It was a no-op since the column already existed with `DEFAULT now()`.
- **`moveCursor` called with undefined boardId.** The Canvas component could fire `moveCursor(undefined, pos)` if a pointer event fired before `boardId` resolved from route data. Added a `!boardId` guard in `flushCursor()`.
- **Stale E2E assertions.** `e2e/home.spec.js` expected "Sticky Notes" but the UI says "Svelte Realtime Demo". `e2e/board.spec.js` navigated home by clicking "Sticky Notes" text, now uses `a[href="/"]` which works regardless of viewport size.

### Under the Hood

- All arrangement actions (tidy, rearrange, shuffle, group-by-author) produce the same events in the same order. The only change is that they are sent as a single batch frame instead of N+1 individual publishes. No client-side changes needed.
- The `close` hook now delegates to `presence.hooks.close()` and `cursor.hooks.close()` instead of calling `presence.hooks.close()` and `cursor.remove()` separately. Same behavior, uses the standardized hooks pattern from extensions 0.4.0.
- The `subscribe` hook now calls both `presence.hooks.subscribe()` and `cursor.hooks.subscribe()`. Previously only presence was wired up. The cursor hook is a no-op for non-cursor topics.
- The circuit breaker has zero overhead when Redis is healthy. It only changes behavior after 5 consecutive failures.
- The advisory lock uses `pg_try_advisory_lock` (non-blocking). If the lock is held by another replica, the cron returns immediately without querying for stale boards.
- The `assertPlainObject` guard runs before any field access in `validateBoardFields` and `validateNoteFields`. No performance impact on valid payloads.
- Package version bumped from 0.0.3 to 0.4.0 to align with the upstream stack.

---

## [0.0.3] - 2026-03-17

### Added

- Sort boards by online user count on the home page (most active first)
- Fix drag snap-back after note release

## [0.0.2] - 2026-03-16

### Added

- Mobile optimizations (touch drag, responsive navbar, double-tap create)

## [0.0.1] - 2026-03-15

### Added

- Initial release: collaborative sticky notes with real-time sync
- Board CRUD with 1-hour TTL and automatic cleanup
- Note operations (create, edit, drag, delete, color, z-order)
- Undo/redo with history tracking
- Presence tracking (global and per-board)
- Live cursor overlay with Canvas 2D rendering
- Activity ticker (ephemeral, latest merge)
- Board settings (title, background color)
- FAB menu (tidy, rearrange by color, shuffle, group by author)
- Batch SQL for arrangement actions
- Rate limiting (100 RPCs / 10s, drag/cursor excluded)
- Identity system (random name + color, cookie-based)
- Redis-backed pub/sub for multi-instance deployment
- PostgreSQL + in-memory dual database implementation
- E2E test suite (Playwright)
- Docker Compose deployment with certbot TLS
- 1000-cursor stress test
