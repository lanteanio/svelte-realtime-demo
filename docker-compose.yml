services:
  app:
    build: .
    ports:
      - "443:3000"
    volumes:
      - certs:/etc/letsencrypt:ro
    environment:
      DATABASE_URL: postgres://postgres:${POSTGRES_PASSWORD:?required}@postgres:5432/stickynotes
      REDIS_URL: redis://redis:6379
      PORT: "3000"
      ORIGIN: https://${DOMAIN:?required}
      SSL_CERT: /etc/letsencrypt/live/${DOMAIN}/fullchain.pem
      SSL_KEY: /etc/letsencrypt/live/${DOMAIN}/privkey.pem
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped

  certbot:
    image: certbot/certbot
    volumes:
      - certs:/etc/letsencrypt
    ports:
      - "80:80"
    entrypoint: sh
    command:
      - -c
      - |
        trap exit TERM
        while :; do
          certbot renew --standalone --preferred-challenges http
          sleep 12h & wait $${!}
        done
    restart: unless-stopped

  postgres:
    build:
      context: .
      dockerfile: Dockerfile.postgres
    environment:
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?required}
      POSTGRES_DB: stickynotes
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d stickynotes"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 30s
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    command: redis-server --protected-mode no --appendonly yes
    volumes:
      - redisdata:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
    restart: unless-stopped

volumes:
  certs:
  pgdata:
  redisdata:
